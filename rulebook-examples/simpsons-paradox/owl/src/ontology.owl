@prefix rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .
@prefix owl: <http://www.w3.org/2002/07/owl#> .
@prefix xsd: <http://www.w3.org/2001/XMLSchema#> .
@prefix effortless-ntwf: <https://w3id.org/effortless-ntwf#> .

<https://w3id.org/effortless-ntwf#> a owl:Ontology ;
    rdfs:label "NTWF Ontology (Effortless rendering)" ;
    rdfs:comment "Generated by Effortless from simpsons-paradox-rulebook.json. Vocabulary: Jessica Talisman NTWF." .

# === Class: AllocationSweep ===
effortless-ntwf:AllocationSweep a owl:Class ;
    rdfs:label "AllocationSweep" ;
    rdfs:comment "Table: AllocationSweep — parametric sweep of treatment-A allocation fraction within each study's most confounded stratum (see SweepStudyConfig). Ten fractions from 0.05 to 0.95 per study. SweepCorrectedGap is invariant; SweepPooledGap wanders." .

effortless-ntwf:sweepId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:AllocationSweep ;
    rdfs:range xsd:string ;
    rdfs:label "SweepId" ;
    rdfs:comment "Unique row identifier: <study>-f<pct> where pct is AllocFractionA*100." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:studyId a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "StudyId" ;
    rdfs:comment "The study being swept — all 238 studies in the corpus." .

effortless-ntwf:allocFractionA a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:AllocationSweep ;
    rdfs:range xsd:string ;
    rdfs:label "AllocFractionA" ;
    rdfs:comment "Fraction of the sweep-stratum cases assigned to treatment A. Ranges from 0.05 to 0.95. The sweep stratum is the one whose allocation was most confounded in the original study." .

effortless-ntwf:isOriginal a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:AllocationSweep ;
    rdfs:range xsd:boolean ;
    rdfs:label "IsOriginal" ;
    rdfs:comment "TRUE for the row closest to the actual original study allocation." .
    # calculated: calculated

effortless-ntwf:nSweepStratumTotal a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "NSweepStratumTotal" ;
    rdfs:comment "Total cases in the sweep stratum (the stratum whose allocation is being varied). Fixed per study." .
    # calculated: calculated

effortless-ntwf:nSweepA a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:AllocationSweep ;
    rdfs:range xsd:string ;
    rdfs:label "NSweepA" ;
    rdfs:comment "Cases in the sweep stratum assigned to treatment A = ROUND(NSweepStratumTotal * AllocFractionA)." .
    # calculated: calculated

effortless-ntwf:nSweepB a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:AllocationSweep ;
    rdfs:range xsd:string ;
    rdfs:label "NSweepB" ;
    rdfs:comment "Cases in the sweep stratum assigned to treatment B = NSweepStratumTotal - NSweepA." .
    # calculated: calculated

effortless-ntwf:nFixedA a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "NFixedA" ;
    rdfs:comment "Total A-treatment cases in all NON-swept strata (held fixed across sweep rows). For kidney-1986 = 87 (small-stratum A). For berkeley-1973 = 1302 (depts B+C+D male). For compressed-synthetic = 80 (easy-stratum A). For balanced-synthetic = 100 (hard-stratum A)." .
    # calculated: calculated

effortless-ntwf:nFixedB a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "NFixedB" ;
    rdfs:comment "Total B-treatment cases in all NON-swept strata (held fixed across sweep rows). For kidney-1986 = 270 (small-stratum B). For berkeley-1973 = 993 (depts B+C+D female). For compressed-synthetic = 120 (easy-stratum B). For balanced-synthetic = 100 (hard-stratum B)." .
    # calculated: calculated

effortless-ntwf:sweepRateA a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SweepRateA" ;
    rdfs:comment "Per-cell success rate for treatment A in the sweep stratum. Fixed per study." .
    # calculated: calculated

effortless-ntwf:sweepRateB a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SweepRateB" ;
    rdfs:comment "Per-cell success rate for treatment B in the sweep stratum. Fixed per study." .
    # calculated: calculated

effortless-ntwf:fixedRateA a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "FixedRateA" ;
    rdfs:comment "Weighted success rate for treatment A across all fixed (non-swept) strata. Constant per study." .
    # calculated: calculated

effortless-ntwf:fixedRateB a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "FixedRateB" ;
    rdfs:comment "Weighted success rate for treatment B across all fixed (non-swept) strata. Constant per study." .
    # calculated: calculated

effortless-ntwf:sweepPooledRateA a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:AllocationSweep ;
    rdfs:range xsd:string ;
    rdfs:label "SweepPooledRateA" ;
    rdfs:comment "Pooled success rate for treatment A = (NSweepA * SweepRateA + NFixedA * FixedRateA) / (NSweepA + NFixedA). Changes as AllocFractionA varies." .
    # calculated: calculated

effortless-ntwf:sweepPooledRateB a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:AllocationSweep ;
    rdfs:range xsd:string ;
    rdfs:label "SweepPooledRateB" ;
    rdfs:comment "Pooled success rate for treatment B = (NSweepB * SweepRateB + NFixedB * FixedRateB) / (NSweepB + NFixedB). Changes as AllocFractionA varies." .
    # calculated: calculated

effortless-ntwf:sweepPooledGap a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:AllocationSweep ;
    rdfs:range xsd:string ;
    rdfs:label "SweepPooledGap" ;
    rdfs:comment "SweepPooledRateA minus SweepPooledRateB. Wanders as AllocFractionA varies because pooled rates mix stratum sizes unequally. Crosses zero at some allocation (the paradox reversal point)." .
    # calculated: calculated

effortless-ntwf:sweepCorrectedGap a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SweepCorrectedGap" ;
    rdfs:comment "Allocation-free signal: sum over strata of (n_s/N) * (rate_As - rate_Bs). Constant across the sweep by construction — the allocation variable does not appear. This is the conserved quantity under re-stratification." .
    # calculated: calculated

effortless-ntwf:allocationDistortionWitness a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:AllocationSweep ;
    rdfs:range xsd:string ;
    rdfs:label "AllocationDistortionWitness" ;
    rdfs:comment "SweepPooledGap minus SweepCorrectedGap. The allocation distortion: how much the pooled signal has been inflated or deflated by differential stratum allocation. Zero only when allocation is balanced." .
    # calculated: calculated

# === Class: CandidateStudyCatalog ===
effortless-ntwf:CandidateStudyCatalog a owl:Class ;
    rdfs:label "CandidateStudyCatalog" ;
    rdfs:comment "Table: CandidateStudyCatalog — curated backlog of published Simpson's-paradox-eligible studies. Tracks import status, expected distortion type, and encoding metadata before CaseCells exist." .

effortless-ntwf:candidateId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:string ;
    rdfs:label "CandidateId" ;
    rdfs:comment "Unique slug for this catalog entry." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:proposedStudyId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:string ;
    rdfs:label "ProposedStudyId" ;
    rdfs:comment "Stable slug to assign in Studies when imported." .

effortless-ntwf:title a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Title" ;
    rdfs:comment "Human-readable study title." .

effortless-ntwf:citation a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:string ;
    rdfs:label "Citation" ;
    rdfs:comment "Primary publication citation." .

effortless-ntwf:sourceUrl a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SourceUrl" ;
    rdfs:comment "URL to source paper or data." .

effortless-ntwf:domain a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Domain" ;
    rdfs:comment "Domain tag: medicine, epidemiology, legal, sports, education, economics, social-science." .

effortless-ntwf:stratumVariableName a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:string ;
    rdfs:label "StratumVariableName" ;
    rdfs:comment "Pre-registered stratifying variable name for encoding." .

effortless-ntwf:expectedDistortionType a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:string ;
    rdfs:label "ExpectedDistortionType" ;
    rdfs:comment "Hypothesized DistortionType after classification: A, B, C+, C-, D, or unknown." .

effortless-ntwf:ingestionStatus a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:string ;
    rdfs:label "IngestionStatus" ;
    rdfs:comment "imported | candidate | blocked | rejected" .

effortless-ntwf:priority a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:integer ;
    rdfs:label "Priority" ;
    rdfs:comment "1=high priority encode next; 5=blocked/duplicate." .

effortless-ntwf:stratumCountEstimate a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:integer ;
    rdfs:label "StratumCountEstimate" ;
    rdfs:comment "Expected number of strata (minimum 2)." .

effortless-ntwf:dataSourceNote a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:string ;
    rdfs:label "DataSourceNote" ;
    rdfs:comment "Where to find cell counts and encoding notes." .

effortless-ntwf:linkedStudyId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:string ;
    rdfs:label "LinkedStudyId" ;
    rdfs:comment "Studies.StudyId when IngestionStatus=imported." .

effortless-ntwf:publicationYear a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "PublicationYear" ;
    rdfs:comment "Publication year when known." .

effortless-ntwf:isReadyToEncode a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:boolean ;
    rdfs:label "IsReadyToEncode" ;
    rdfs:comment "TRUE when candidate has citation, proposed id, and stratum variable — ready for an import session." .
    # calculated: calculated

effortless-ntwf:isImported a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:boolean ;
    rdfs:label "IsImported" ;
    rdfs:comment "TRUE when already in corpus." .
    # calculated: calculated

effortless-ntwf:observedDistortionType a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:string ;
    rdfs:label "ObservedDistortionType" ;
    rdfs:comment "Actual DistortionType from TreatmentRankings when imported." .
    # calculated: lookup

effortless-ntwf:typePredictionMatch a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:boolean ;
    rdfs:label "TypePredictionMatch" ;
    rdfs:comment "TRUE when observed type matches expected (imported rows only)." .
    # calculated: calculated

effortless-ntwf:expectedSignFlip a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:boolean ;
    rdfs:label "ExpectedSignFlip" ;
    rdfs:comment "TRUE when curator tagged ExpectedDistortionType as A or B (sign-flip expectation)." .
    # calculated: calculated

effortless-ntwf:observedSignFlipType a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:boolean ;
    rdfs:label "ObservedSignFlipType" ;
    rdfs:comment "TRUE when observed DistortionType is A or B (imported rows only)." .
    # calculated: calculated

effortless-ntwf:signFlipPredictionMatch a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:boolean ;
    rdfs:label "SignFlipPredictionMatch" ;
    rdfs:comment "TRUE when Expected A∪B and observed is A∪B — weaker flip-type prediction test." .
    # calculated: calculated

effortless-ntwf:dataAcquisitionStatus a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:string ;
    rdfs:label "DataAcquisitionStatus" ;
    rdfs:comment "not_started | downloaded | manual_only | blocked" .

effortless-ntwf:reversalMechanism a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:string ;
    rdfs:label "ReversalMechanism" ;
    rdfs:comment "Plain-language reversal mechanism from expansion plan." .

effortless-ntwf:paradoxConfirmation a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:string ;
    rdfs:label "ParadoxConfirmation" ;
    rdfs:comment "confirmed | plausible | pending | not_applicable" .

effortless-ntwf:expansionWave a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:string ;
    rdfs:label "ExpansionWave" ;
    rdfs:comment "Catalog wave tag; loop-67 expansion rows = expansion-wave-1." .

effortless-ntwf:isDataReady a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CandidateStudyCatalog ;
    rdfs:range xsd:boolean ;
    rdfs:label "IsDataReady" ;
    rdfs:comment "TRUE when stratified data is downloaded or available from published tables." .
    # calculated: calculated

# === Class: CaseCells ===
effortless-ntwf:CaseCells a owl:Class ;
    rdfs:label "CaseCells" ;
    rdfs:comment "Table: CaseCells — the raw leaves of the DAG. One row per (study, stratum, treatment) combination recording the aggregate count of successes and total cases. Every derived fact in the model traces back to these numbers. Nothing above this row is a raw input." .

effortless-ntwf:caseCellId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CaseCells ;
    rdfs:range xsd:string ;
    rdfs:label "CaseCellId" ;
    rdfs:comment "Unique slug (e.g. kidney-1986-small-A)." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:study a owl:ObjectProperty ;
    rdfs:range effortless-ntwf:Studies ;
    rdfs:label "Study" ;
    rdfs:comment "FK to the parent Study." .

effortless-ntwf:stratumLabel a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "StratumLabel" ;
    rdfs:comment "The stratum this cell belongs to (matches Strata.StratumLabel)." .

effortless-ntwf:treatmentLabel a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TreatmentLabel" ;
    rdfs:comment "The treatment applied in this cell (matches Treatments.TreatmentLabel)." .

effortless-ntwf:successes a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CaseCells ;
    rdfs:range xsd:integer ;
    rdfs:label "Successes" ;
    rdfs:comment "Number of patients in this cell who had a successful outcome. RAW INPUT — never derived." .

effortless-ntwf:cases a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CaseCells ;
    rdfs:range xsd:integer ;
    rdfs:label "Cases" ;
    rdfs:comment "Total number of patients in this cell. RAW INPUT — never derived." .

effortless-ntwf:cellSuccessRate a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CaseCells ;
    rdfs:range xsd:string ;
    rdfs:label "CellSuccessRate" ;
    rdfs:comment "Success rate within this cell: Successes / Cases. The first derived fact in the DAG. Fixed loop-90: this rulebook's IF(x=0,\"\",...) idiom transpiles to a bare ''::numeric cast that errors at runtime for the one CaseCells row with Cases=0 (surfaced only once _erb_tr_metrics stopped masking it). Returns 0 for the degenerate zero-cases cell instead." .
    # calculated: calculated

effortless-ntwf:totalCasesForTreatment a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CaseCells ;
    rdfs:range xsd:integer ;
    rdfs:label "TotalCasesForTreatment" ;
    rdfs:comment "Total cases across ALL strata for this treatment in this study — the denominator for TreatmentExposureFraction." .
    # calculated: aggregation

effortless-ntwf:treatmentExposureFraction a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CaseCells ;
    rdfs:range xsd:string ;
    rdfs:label "TreatmentExposureFraction" ;
    rdfs:comment "Fraction of this treatment's total cases that fall in this stratum: Cases / TotalCasesForTreatment. High imbalance across strata is the mechanism of confounding — when treatments are allocated very differently across strata, the pooled rate conflates treatment effect with stratum difficulty." .
    # calculated: calculated

effortless-ntwf:isValidIngestionCell a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CaseCells ;
    rdfs:range xsd:boolean ;
    rdfs:label "IsValidIngestionCell" ;
    rdfs:comment "TRUE when Cases>=0, Successes>=0, and Successes<=Cases (loop-92: a stratum-treatment combination can legitimately have zero observed cases in real source data -- e.g. rdatasets-mtcars-engine has zero 8-cylinder straight-engine cars -- so Cases=0 is a valid ingestion cell, not a defect)." .
    # calculated: calculated

# === Class: Conclusions ===
effortless-ntwf:Conclusions a owl:Class ;
    rdfs:label "Conclusions" ;
    rdfs:comment "Table: Conclusions — the substantive epistemic findings that this model has produced or is designed to produce. Each row is one conclusion: a claim about the domain, about the instrument, or about the methodology that is either already witnessed or is the target of a planned loop." .

effortless-ntwf:conclusionId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Conclusions ;
    rdfs:range xsd:string ;
    rdfs:label "ConclusionId" ;
    rdfs:comment "Unique slug (e.g. conc-01-paradox-is-derived)." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label, mirrors ConclusionId." .
    # calculated: calculated

effortless-ntwf:category a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Conclusions ;
    rdfs:range xsd:string ;
    rdfs:label "Category" ;
    rdfs:comment "domain | instrument | methodology | open-question | corpus-theorem" .

effortless-ntwf:status a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Status" ;
    rdfs:comment "witnessed | planned | open" .

effortless-ntwf:reportTier a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Conclusions ;
    rdfs:range xsd:string ;
    rdfs:label "ReportTier" ;
    rdfs:comment "critical | supporting | transient — controls how much space this conclusion gets in the generated PDF/summary report. critical = full section with evidence + chart; supporting = 1-2 line bullet; transient = folded into a single compact list." .

effortless-ntwf:validatingHypothesis a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Conclusions ;
    rdfs:range xsd:string ;
    rdfs:label "ValidatingHypothesis" ;
    rdfs:comment "FK → DiscoveryHypotheses.HypothesisId. The pre-registered hypothesis whose live DiscoveryFindings.IsConfirmed value is the current-truth check for this conclusion — set on theorem/corpus-theorem conclusions so a report can distinguish a conclusion's archived prose (may say 'Proved') from its live status (may since be contradicted by corpus growth). Null when no single hypothesis tests this exact claim." .

effortless-ntwf:liveStatusConfirmed a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Conclusions ;
    rdfs:range xsd:boolean ;
    rdfs:label "LiveStatusConfirmed" ;
    rdfs:comment "Lookup: DiscoveryFindings.IsConfirmed via ValidatingHypothesis — the current pass/fail state of the hypothesis backing this conclusion, recomputed on every build from live corpus data. NULL when ValidatingHypothesis is not set (no live check exists for this conclusion)." .
    # calculated: lookup

effortless-ntwf:liveStatusObservedMetric a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Conclusions ;
    rdfs:range xsd:string ;
    rdfs:label "LiveStatusObservedMetric" ;
    rdfs:comment "Lookup: DiscoveryFindings.ObservedMetric via ValidatingHypothesis — the current observed values for the hypothesis backing this conclusion, so a reader can see exactly why it currently passes or fails." .
    # calculated: lookup

effortless-ntwf:title a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Title" ;
    rdfs:comment "One-line statement of the conclusion." .

effortless-ntwf:evidence a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Evidence" ;
    rdfs:comment "The specific DB query result, loop outcome, or argument that establishes or will establish this conclusion." .

effortless-ntwf:witnessedInLoop a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "WitnessedInLoop" ;
    rdfs:comment "LoopId where this conclusion was first witnessed (if status=witnessed)." .

effortless-ntwf:targetLoop a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Conclusions ;
    rdfs:range xsd:string ;
    rdfs:label "TargetLoop" ;
    rdfs:comment "LoopId that is designed to witness this conclusion (if status=planned)." .

effortless-ntwf:traditionId a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionId" ;
    rdfs:comment "FK → ResearchTraditions.TraditionId. The tradition whose theoretical framework this conclusion primarily vindicates, refines, or challenges." .

effortless-ntwf:researcherId a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "ResearcherId" ;
    rdfs:comment "FK → Researchers.ResearcherId. The researcher whose specific claim, definition, or framework this conclusion directly affirms." .

effortless-ntwf:challengesResearcher a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Conclusions ;
    rdfs:range xsd:string ;
    rdfs:label "ChallengesResearcher" ;
    rdfs:comment "FK → Researchers.ResearcherId. A researcher whose position this conclusion refutes or qualifies (e.g. conc-03 refutes Blyth's unanimity criterion as the primary definition)." .

effortless-ntwf:traditionName a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionName" ;
    rdfs:comment "Lookup: ResearchTraditions.Name via TraditionId." .
    # calculated: lookup

effortless-ntwf:traditionKeyClaimSummary a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionKeyClaimSummary" ;
    rdfs:comment "Lookup: ResearchTraditions.KeyClaim via TraditionId — shows which tradition's central claim this conclusion validates or refines." .
    # calculated: lookup

effortless-ntwf:researcherName a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Conclusions ;
    rdfs:range xsd:string ;
    rdfs:label "ResearcherName" ;
    rdfs:comment "Lookup: Researchers.Name via ResearcherId." .
    # calculated: lookup

effortless-ntwf:researcherAffiliation a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Conclusions ;
    rdfs:range xsd:string ;
    rdfs:label "ResearcherAffiliation" ;
    rdfs:comment "Lookup: Researchers.Affiliation via ResearcherId." .
    # calculated: lookup

effortless-ntwf:challengesResearcherName a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Conclusions ;
    rdfs:range xsd:string ;
    rdfs:label "ChallengesResearcherName" ;
    rdfs:comment "Lookup: Researchers.Name via ChallengesResearcher." .
    # calculated: lookup

effortless-ntwf:witnessedInLoopTitle a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Conclusions ;
    rdfs:range xsd:string ;
    rdfs:label "WitnessedInLoopTitle" ;
    rdfs:comment "Lookup: Loops.Title via WitnessedInLoop." .
    # calculated: lookup

effortless-ntwf:witnessedInLoopCommitHash a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Conclusions ;
    rdfs:range xsd:string ;
    rdfs:label "WitnessedInLoopCommitHash" ;
    rdfs:comment "Lookup: Loops.CommitHash via WitnessedInLoop — git checkout this SHA to replay the instrument state when this conclusion was first witnessed." .
    # calculated: lookup

effortless-ntwf:witnessedInLoopCommitShort a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Conclusions ;
    rdfs:range xsd:string ;
    rdfs:label "WitnessedInLoopCommitShort" ;
    rdfs:comment "Lookup: Loops.CommitShort via WitnessedInLoop." .
    # calculated: lookup

effortless-ntwf:witnessedInLoopCommitDate a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Conclusions ;
    rdfs:range xsd:string ;
    rdfs:label "WitnessedInLoopCommitDate" ;
    rdfs:comment "Lookup: Loops.CommitDate via WitnessedInLoop." .
    # calculated: lookup

effortless-ntwf:witnessedInLoopGitTag a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Conclusions ;
    rdfs:range xsd:string ;
    rdfs:label "WitnessedInLoopGitTag" ;
    rdfs:comment "Lookup: Loops.GitTag via WitnessedInLoop — preferred replay entry point when a milestone tag exists." .
    # calculated: lookup

effortless-ntwf:invariantProtectingCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Conclusions ;
    rdfs:range xsd:integer ;
    rdfs:label "InvariantProtectingCount" ;
    rdfs:comment "Count of InvariantChecks whose ProtectsConclusion FK points to this conclusion — how many algebraic invariants stand behind this finding." .
    # calculated: aggregation

# === Class: ConfounderDistortionTimeline ===
effortless-ntwf:ConfounderDistortionTimeline a owl:Class ;
    rdfs:label "ConfounderDistortionTimeline" ;
    rdfs:comment "Table: ConfounderDistortionTimeline -- derived geometry at the (ConfounderIdentity x ValidTimeDecade) grain. Each row aggregates the studies whose real-world cohort period (valid-time, not publication year) falls in that decade and that carry the given confounder archetype, tracking whether the corpus drifts toward clean Type-D (non-reversing, zero-allocation-distortion) outcomes over time and whether that drift is confounder-specific. Principled materialization per CLAUDE.md (N-way join over a sealed, bounded study set) -- refreshed via refresh_confounder_distortion_timeline(), same pattern as IdentityClusterSummaries/IdentityDomainCells." .

effortless-ntwf:cellId a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "CellId" ;
    rdfs:comment "Unique slug: confounder-identity-id + '-x-' + valid-time decade (e.g. id-disease-severity-x-1980)." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:confounderIdentity a owl:ObjectProperty ;
    rdfs:range effortless-ntwf:ConfounderIdentities ;
    rdfs:label "ConfounderIdentity" ;
    rdfs:comment "FK -> ConfounderIdentities." .

effortless-ntwf:validTimeDecade a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "ValidTimeDecade" ;
    rdfs:comment "Studies.ValidTimeDecade bucket this cell aggregates over (e.g. 1980 covers valid-time midpoints 1980-1989)." .

effortless-ntwf:studyCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "StudyCount" ;
    rdfs:comment "Studies carrying this ConfounderIdentity whose ValidTimeDecade falls in this bucket (refresh_confounder_distortion_timeline())." .

effortless-ntwf:typeDCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "TypeDCount" ;
    rdfs:comment "Of those, count with TreatmentRankings.DistortionType = 'D' (clean, non-reversing)." .

effortless-ntwf:signFlipCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ConfounderDistortionTimeline ;
    rdfs:range xsd:integer ;
    rdfs:label "SignFlipCount" ;
    rdfs:comment "Of those, count with TreatmentRankings.IsSignFlip = TRUE." .

effortless-ntwf:sumSignalPurity a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ConfounderDistortionTimeline ;
    rdfs:range xsd:string ;
    rdfs:label "SumSignalPurity" ;
    rdfs:comment "Sum of TreatmentRankings.SignalPurity across cell studies (numerator for MeanSignalPurity; summed rather than pre-averaged so the aggregate survives refresh re-runs without drift)." .

effortless-ntwf:sumAllocationDistortion a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ConfounderDistortionTimeline ;
    rdfs:range xsd:string ;
    rdfs:label "SumAllocationDistortion" ;
    rdfs:comment "Sum of TreatmentRankings.AllocationDistortion across cell studies (numerator for MeanAllocationDistortion)." .

effortless-ntwf:typeDFraction a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ConfounderDistortionTimeline ;
    rdfs:range xsd:string ;
    rdfs:label "TypeDFraction" ;
    rdfs:comment "TypeDCount / StudyCount." .
    # calculated: calculated

effortless-ntwf:signFlipFraction a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ConfounderDistortionTimeline ;
    rdfs:range xsd:string ;
    rdfs:label "SignFlipFraction" ;
    rdfs:comment "SignFlipCount / StudyCount." .
    # calculated: calculated

effortless-ntwf:meanSignalPurity a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ConfounderDistortionTimeline ;
    rdfs:range xsd:string ;
    rdfs:label "MeanSignalPurity" ;
    rdfs:comment "SumSignalPurity / StudyCount." .
    # calculated: calculated

effortless-ntwf:meanAllocationDistortion a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ConfounderDistortionTimeline ;
    rdfs:range xsd:string ;
    rdfs:label "MeanAllocationDistortion" ;
    rdfs:comment "SumAllocationDistortion / StudyCount." .
    # calculated: calculated

effortless-ntwf:identityDisplayName a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "IdentityDisplayName" ;
    rdfs:comment "Lookup: ConfounderIdentities.DisplayName via ConfounderIdentity." .
    # calculated: lookup

effortless-ntwf:mechanismClass a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "MechanismClass" ;
    rdfs:comment "Lookup: ConfounderIdentities.MechanismClass via ConfounderIdentity — the archetype group used to test whether Type-D drift is confounder-specific." .
    # calculated: lookup

effortless-ntwf:identityEarliestDecade a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ConfounderDistortionTimeline ;
    rdfs:range xsd:integer ;
    rdfs:label "IdentityEarliestDecade" ;
    rdfs:comment "Earliest ValidTimeDecade present in this table for this ConfounderIdentity." .
    # calculated: aggregation

effortless-ntwf:identityLatestDecade a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ConfounderDistortionTimeline ;
    rdfs:range xsd:integer ;
    rdfs:label "IdentityLatestDecade" ;
    rdfs:comment "Latest ValidTimeDecade present in this table for this ConfounderIdentity." .
    # calculated: aggregation

effortless-ntwf:identityEarliestTypeDFraction a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ConfounderDistortionTimeline ;
    rdfs:range xsd:string ;
    rdfs:label "IdentityEarliestTypeDFraction" ;
    rdfs:comment "TypeDFraction of this ConfounderIdentity's earliest decade cell -- the start point for the drift comparison." .
    # calculated: lookup

effortless-ntwf:identityLatestTypeDFraction a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ConfounderDistortionTimeline ;
    rdfs:range xsd:string ;
    rdfs:label "IdentityLatestTypeDFraction" ;
    rdfs:comment "TypeDFraction of this ConfounderIdentity's latest decade cell -- the end point for the drift comparison." .
    # calculated: lookup

effortless-ntwf:driftDirection a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ConfounderDistortionTimeline ;
    rdfs:range xsd:string ;
    rdfs:label "DriftDirection" ;
    rdfs:comment "rising | falling | flat | single-decade -- compares this ConfounderIdentity's earliest-decade TypeDFraction to its latest-decade TypeDFraction across the whole table (same value on every row for a given ConfounderIdentity by construction)." .
    # calculated: calculated

# === Class: ConfounderIdentities ===
effortless-ntwf:ConfounderIdentities a owl:Class ;
    rdfs:label "ConfounderIdentities" ;
    rdfs:comment "Table: ConfounderIdentities — canonical cross-study archetypes for stratum variables. Each identity names a reusable confounding mechanism accountable across the corpus (loop-80)." .

effortless-ntwf:confounderIdentityId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ConfounderIdentities ;
    rdfs:range xsd:string ;
    rdfs:label "ConfounderIdentityId" ;
    rdfs:comment "Unique slug (e.g. id-age-composition)." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:displayName a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "DisplayName" ;
    rdfs:comment "Human-readable archetype name." .

effortless-ntwf:mechanismClass a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "MechanismClass" ;
    rdfs:comment "Mechanism taxonomy token." .

effortless-ntwf:description a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Description" ;
    rdfs:comment "Narrative description of confounding mechanism." .

effortless-ntwf:policyDefault a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ConfounderIdentities ;
    rdfs:range xsd:string ;
    rdfs:label "PolicyDefault" ;
    rdfs:comment "Default analytic policy for this confounding archetype." .

effortless-ntwf:studyCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "StudyCount" ;
    rdfs:comment "Studies with a StratumVariable mapped to this identity." .
    # calculated: aggregation

# === Class: CorpusBalance ===
effortless-ntwf:CorpusBalance a owl:Class ;
    rdfs:label "CorpusBalance" .

effortless-ntwf:balanceId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusBalance ;
    rdfs:range xsd:string ;
    rdfs:label "BalanceId" ;
    rdfs:comment "Primary key (singleton: 'corpus-balance')" .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "=BalanceId" .
    # calculated: calculated

effortless-ntwf:totalStudyCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusBalance ;
    rdfs:range xsd:integer ;
    rdfs:label "TotalStudyCount" ;
    rdfs:comment "Total studies in corpus (Simpson + control). Fixed loop-90: was authored with COUNTA/bareword refs and an uppercase Formula key, which the transpiler cannot parse. COUNTIFS(...,\"<>\") also mistranslates (known transpiler bug: literal '<>' string instead of IS NOT NULL), so this is expressed as SimpsonStudyCount + ControlStudyCount instead." .
    # calculated: calculated

effortless-ntwf:simpsonStudyCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusBalance ;
    rdfs:range xsd:integer ;
    rdfs:label "SimpsonStudyCount" ;
    rdfs:comment "Studies where IsControlStudy=FALSE. Fixed loop-90 (see TotalStudyCount)." .
    # calculated: aggregation

effortless-ntwf:controlStudyCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusBalance ;
    rdfs:range xsd:integer ;
    rdfs:label "ControlStudyCount" ;
    rdfs:comment "Studies where IsControlStudy=TRUE. Fixed loop-90 (see TotalStudyCount)." .
    # calculated: aggregation

effortless-ntwf:controlFraction a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusBalance ;
    rdfs:range xsd:string ;
    rdfs:label "ControlFraction" ;
    rdfs:comment "ControlStudyCount / TotalStudyCount. Fixed loop-90 (see TotalStudyCount); returns 0 for the degenerate empty-corpus edge instead of \"\" (see TreatmentRankings.DistortionRatio for why IF(x=0,\"\",...) is unsafe on a numeric-typed field)." .
    # calculated: calculated

# === Class: CorpusCatalogSummary ===
effortless-ntwf:CorpusCatalogSummary a owl:Class ;
    rdfs:label "CorpusCatalogSummary" ;
    rdfs:comment "Table: CorpusCatalogSummary — one row witnessing import-backlog readiness for bulk encoding sessions." .

effortless-ntwf:catalogSummaryId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusCatalogSummary ;
    rdfs:range xsd:string ;
    rdfs:label "CatalogSummaryId" ;
    rdfs:comment "Unique identifier." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:totalCatalogEntries a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusCatalogSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "TotalCatalogEntries" ;
    rdfs:comment "All catalog rows. Fixed loop-90 (see IngestionSummary.ProtocolItemCount for why COUNTIFS(...,\"<>\") is unsafe)." .
    # calculated: aggregation

effortless-ntwf:importedCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusCatalogSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "ImportedCount" ;
    rdfs:comment "Rows already imported." .
    # calculated: aggregation

effortless-ntwf:candidateCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusCatalogSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "CandidateCount" ;
    rdfs:comment "Rows queued for import." .
    # calculated: aggregation

effortless-ntwf:blockedCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusCatalogSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "BlockedCount" ;
    rdfs:comment "Duplicate or blocked rows." .
    # calculated: aggregation

effortless-ntwf:readyToEncodeCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "ReadyToEncodeCount" ;
    rdfs:comment "Candidates passing IsReadyToEncode." .
    # calculated: aggregation

effortless-ntwf:highPriorityCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusCatalogSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "HighPriorityCount" ;
    rdfs:comment "Candidates with Priority <= 2." .
    # calculated: aggregation

effortless-ntwf:importSessionReady a owl:DatatypeProperty ;
    rdfs:range xsd:boolean ;
    rdfs:label "ImportSessionReady" ;
    rdfs:comment "TRUE when backlog is large enough for a bulk import session." .
    # calculated: calculated

effortless-ntwf:typePredictionMatchCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "TypePredictionMatchCount" ;
    rdfs:comment "Imported catalog rows where ObservedDistortionType exactly matches ExpectedDistortionType." .
    # calculated: aggregation

effortless-ntwf:typePredictionMismatchCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "TypePredictionMismatchCount" ;
    rdfs:comment "Imported catalog rows where exact type prediction failed." .
    # calculated: aggregation

effortless-ntwf:signFlipPredictionEligibleCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusCatalogSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "SignFlipPredictionEligibleCount" ;
    rdfs:comment "Imported rows where ExpectedDistortionType was A or B." .
    # calculated: aggregation

effortless-ntwf:signFlipPredictionMatchCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusCatalogSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "SignFlipPredictionMatchCount" ;
    rdfs:comment "Expected A∪B rows where observed type is also A∪B." .
    # calculated: aggregation

effortless-ntwf:typePredictionMatchRate a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TypePredictionMatchRate" ;
    rdfs:comment "Exact-match rate across imported catalog rows." .
    # calculated: calculated

effortless-ntwf:signFlipPredictionMatchRate a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SignFlipPredictionMatchRate" ;
    rdfs:comment "Sign-flip-type match rate among Expected A∪B rows." .
    # calculated: calculated

effortless-ntwf:catalogWitnessNote a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "CatalogWitnessNote" ;
    rdfs:comment "Human-readable readiness summary." .
    # calculated: calculated

effortless-ntwf:catalogPredictionWitnessNote a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "CatalogPredictionWitnessNote" ;
    rdfs:comment "Loop-65 type-prediction audit summary." .
    # calculated: calculated

effortless-ntwf:dataReadyCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusCatalogSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "DataReadyCount" ;
    rdfs:comment "Candidates with IsDataReady=TRUE." .
    # calculated: aggregation

effortless-ntwf:encodePipelineReadyCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusCatalogSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "EncodePipelineReadyCount" ;
    rdfs:comment "Candidates both data-ready and encode-ready." .
    # calculated: aggregation

effortless-ntwf:expansionCandidateCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusCatalogSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "ExpansionCandidateCount" ;
    rdfs:comment "Loop-67 expansion-wave-1 candidates." .
    # calculated: aggregation

# === Class: CorpusDomains ===
effortless-ntwf:CorpusDomains a owl:Class ;
    rdfs:label "CorpusDomains" ;
    rdfs:comment "Table: CorpusDomains — canonical expansion-domain slugs from corpus-expansion-plan.md (loop-67)." .

effortless-ntwf:domainId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusDomains ;
    rdfs:range xsd:string ;
    rdfs:label "DomainId" ;
    rdfs:comment "Unique slug (matches CandidateStudyCatalog.Domain for expansion wave)." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:displayName a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "DisplayName" ;
    rdfs:comment "Human-readable domain name." .

effortless-ntwf:expansionTarget a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusDomains ;
    rdfs:range xsd:integer ;
    rdfs:label "ExpansionTarget" ;
    rdfs:comment "Target study count from corpus-expansion-plan.md." .

effortless-ntwf:legacyStudiesDomain a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusDomains ;
    rdfs:range xsd:string ;
    rdfs:label "LegacyStudiesDomain" ;
    rdfs:comment "Studies.Domain alias for counting already-imported corpus rows." .

effortless-ntwf:planSourceSection a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:CorpusDomains ;
    rdfs:range xsd:string ;
    rdfs:label "PlanSourceSection" ;
    rdfs:comment "Section heading in corpus-expansion-plan.md." .

# === Class: DiscoveryFindings ===
effortless-ntwf:DiscoveryFindings a owl:Class ;
    rdfs:label "DiscoveryFindings" ;
    rdfs:comment "Table: DiscoveryFindings — witnessed outcomes for each pre-registered DiscoveryHypothesis. IsConfirmed is computed live from ModelSummary aggregates." .

effortless-ntwf:findingId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:DiscoveryFindings ;
    rdfs:range xsd:string ;
    rdfs:label "FindingId" ;
    rdfs:comment "Unique slug; one row per hypothesis." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:hypothesisId a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "HypothesisId" ;
    rdfs:comment "FK → DiscoveryHypotheses.HypothesisId." .

effortless-ntwf:hypothesisStatement a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:DiscoveryFindings ;
    rdfs:range xsd:string ;
    rdfs:label "HypothesisStatement" ;
    rdfs:comment "Lookup: DiscoveryHypotheses.Statement." .
    # calculated: lookup

effortless-ntwf:observedMetric a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:DiscoveryFindings ;
    rdfs:range xsd:string ;
    rdfs:label "ObservedMetric" ;
    rdfs:comment "Witnessed metric value from ModelSummary for this hypothesis." .
    # calculated: calculated

effortless-ntwf:isConfirmed a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:DiscoveryFindings ;
    rdfs:range xsd:boolean ;
    rdfs:label "IsConfirmed" ;
    rdfs:comment "TRUE when the witnessed metric satisfies the pre-registered ExpectedOutcome." .
    # calculated: calculated

effortless-ntwf:evidence a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Evidence" ;
    rdfs:comment "Pass/fail witness string for UI and conclusions." .
    # calculated: calculated

effortless-ntwf:witnessedInLoop a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "WitnessedInLoop" ;
    rdfs:comment "LoopId where this finding was witnessed." .

# === Class: DiscoveryHypotheses ===
effortless-ntwf:DiscoveryHypotheses a owl:Class ;
    rdfs:label "DiscoveryHypotheses" ;
    rdfs:comment "Table: DiscoveryHypotheses — pre-registered empirical claims tested against the corpus at loop-61. Each row states an expected pattern before querying the DAG." .

effortless-ntwf:hypothesisId a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "HypothesisId" ;
    rdfs:comment "Unique slug (e.g. H-latent-d)." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:statement a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Statement" ;
    rdfs:comment "Natural-language claim under test." .

effortless-ntwf:expectedOutcome a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:DiscoveryHypotheses ;
    rdfs:range xsd:string ;
    rdfs:label "ExpectedOutcome" ;
    rdfs:comment "Pass criterion in plain language." .

effortless-ntwf:registeredInLoop a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:DiscoveryHypotheses ;
    rdfs:range xsd:string ;
    rdfs:label "RegisteredInLoop" ;
    rdfs:comment "LoopId where this hypothesis was registered." .

effortless-ntwf:traditionId a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionId" ;
    rdfs:comment "FK → ResearchTraditions.TraditionId." .

effortless-ntwf:epistemicTier a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:DiscoveryHypotheses ;
    rdfs:range xsd:string ;
    rdfs:label "EpistemicTier" ;
    rdfs:comment "consistency-check (definition-linked regression) | corpus-hypothesis (contingent corpus pattern)" .

# === Class: DomainExpansionTargets ===
effortless-ntwf:DomainExpansionTargets a owl:Class ;
    rdfs:label "DomainExpansionTargets" ;
    rdfs:comment "Table: DomainExpansionTargets — per-domain corpus growth targets for bulk import prioritisation." .

effortless-ntwf:domainTargetId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:DomainExpansionTargets ;
    rdfs:range xsd:string ;
    rdfs:label "DomainTargetId" ;
    rdfs:comment "Unique slug." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:domain a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Domain" ;
    rdfs:comment "Domain tag matching Studies.Domain." .

effortless-ntwf:legacyDomainAlias a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:DomainExpansionTargets ;
    rdfs:range xsd:string ;
    rdfs:label "LegacyDomainAlias" ;
    rdfs:comment "Studies.Domain slug for legacy imported corpus rows." .

effortless-ntwf:targetMinCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:DomainExpansionTargets ;
    rdfs:range xsd:integer ;
    rdfs:label "TargetMinCount" ;
    rdfs:comment "Minimum real studies desired in this domain." .

effortless-ntwf:currentImportedCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:DomainExpansionTargets ;
    rdfs:range xsd:integer ;
    rdfs:label "CurrentImportedCount" ;
    rdfs:comment "Real studies in corpus matching LegacyDomainAlias (or Domain when no alias)." .
    # calculated: aggregation

effortless-ntwf:candidateQueuedCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:DomainExpansionTargets ;
    rdfs:range xsd:integer ;
    rdfs:label "CandidateQueuedCount" ;
    rdfs:comment "Catalog candidates in this domain." .
    # calculated: aggregation

effortless-ntwf:projectedCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:DomainExpansionTargets ;
    rdfs:range xsd:integer ;
    rdfs:label "ProjectedCount" ;
    rdfs:comment "Current + queued candidates." .
    # calculated: calculated

effortless-ntwf:gapCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:DomainExpansionTargets ;
    rdfs:range xsd:integer ;
    rdfs:label "GapCount" ;
    rdfs:comment "Studies still needed to hit target after queued imports." .
    # calculated: calculated

effortless-ntwf:isUnderRepresented a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:DomainExpansionTargets ;
    rdfs:range xsd:boolean ;
    rdfs:label "IsUnderRepresented" ;
    rdfs:comment "TRUE when projected count still below target." .
    # calculated: calculated

# === Class: IdentityClusterSummaries ===
effortless-ntwf:IdentityClusterSummaries a owl:Class ;
    rdfs:label "IdentityClusterSummaries" ;
    rdfs:comment "Table: IdentityClusterSummaries — witnessed geometry rollup per ConfounderIdentity (manifest flip, latent-D, avg distortion) — loop-80 open-question substrate." .

effortless-ntwf:identityClusterId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:IdentityClusterSummaries ;
    rdfs:range xsd:string ;
    rdfs:label "IdentityClusterId" ;
    rdfs:comment "Unique slug; one row per ConfounderIdentity cluster." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:confounderIdentity a owl:ObjectProperty ;
    rdfs:range effortless-ntwf:ConfounderIdentities ;
    rdfs:label "ConfounderIdentity" ;
    rdfs:comment "FK → ConfounderIdentities." .

effortless-ntwf:displayName a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "DisplayName" ;
    rdfs:comment "Lookup archetype display name." .
    # calculated: lookup

effortless-ntwf:studyCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "StudyCount" ;
    rdfs:comment "Real studies in cluster (customize SQL)." .

effortless-ntwf:manifestFlipCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "ManifestFlipCount" ;
    rdfs:comment "IsSignFlip=TRUE count in cluster." .

effortless-ntwf:latentFlipCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "LatentFlipCount" ;
    rdfs:comment "LatentFlipPotential=TRUE without manifest flip." .

effortless-ntwf:typeDCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "TypeDCount" ;
    rdfs:comment "Type-D studies in cluster." .

effortless-ntwf:avgAllocationDistortion a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:IdentityClusterSummaries ;
    rdfs:range xsd:string ;
    rdfs:label "AvgAllocationDistortion" ;
    rdfs:comment "Mean allocation distortion in cluster." .

effortless-ntwf:latentFractionAmongTypeD a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "LatentFractionAmongTypeD" ;
    rdfs:comment "Fraction of Type-D rows with LatentFlipPotential." .

effortless-ntwf:domainCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:IdentityClusterSummaries ;
    rdfs:range xsd:integer ;
    rdfs:label "DomainCount" ;
    rdfs:comment "Distinct Studies.Domain values in cluster." .

effortless-ntwf:manifestFlipRate a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "ManifestFlipRate" ;
    rdfs:comment "ManifestFlipCount / StudyCount." .
    # calculated: calculated

# === Class: IdentityDomainCells ===
effortless-ntwf:IdentityDomainCells a owl:Class ;
    rdfs:label "IdentityDomainCells" .

effortless-ntwf:cellId a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "CellId" ;
    rdfs:comment "Unique slug: identity-id + '-x-' + domain slug." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:confounderIdentity a owl:ObjectProperty ;
    rdfs:range effortless-ntwf:ConfounderIdentities ;
    rdfs:label "ConfounderIdentity" ;
    rdfs:comment "FK → ConfounderIdentities." .

effortless-ntwf:domain a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Domain" ;
    rdfs:comment "Studies.Domain value (e.g. medicine, epidemiology)." .

effortless-ntwf:studyCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "StudyCount" ;
    rdfs:comment "Studies with this identity in this domain (customize SQL)." .

effortless-ntwf:manifestFlipCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "ManifestFlipCount" ;
    rdfs:comment "IsSignFlip=TRUE count in cell." .

effortless-ntwf:latentFlipCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "LatentFlipCount" ;
    rdfs:comment "LatentFlipPotential=TRUE without manifest flip." .

effortless-ntwf:typeDCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "TypeDCount" ;
    rdfs:comment "Type-D studies in cell." .

effortless-ntwf:latentFractionAmongTypeD a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "LatentFractionAmongTypeD" ;
    rdfs:comment "Fraction of Type-D rows with LatentFlipPotential in cell." .

effortless-ntwf:manifestFlipRate a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "ManifestFlipRate" ;
    rdfs:comment "ManifestFlipCount / StudyCount." .
    # calculated: calculated

effortless-ntwf:identityDisplayName a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "IdentityDisplayName" ;
    rdfs:comment "Lookup archetype display name." .
    # calculated: lookup

# === Class: IngestionProtocol ===
effortless-ntwf:IngestionProtocol a owl:Class ;
    rdfs:label "IngestionProtocol" ;
    rdfs:comment "Table: IngestionProtocol — the minimal formal contract for adding a new 2×K study to the corpus." .

effortless-ntwf:protocolId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:IngestionProtocol ;
    rdfs:range xsd:string ;
    rdfs:label "ProtocolId" ;
    rdfs:comment "Unique slug for this contract item." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:ruleType a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:IngestionProtocol ;
    rdfs:range xsd:string ;
    rdfs:label "RuleType" ;
    rdfs:comment "required-input | structural | inclusion | exclusion | adapter-metadata" .

effortless-ntwf:targetTable a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TargetTable" ;
    rdfs:comment "Rulebook table this item applies to." .

effortless-ntwf:fieldName a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "FieldName" ;
    rdfs:comment "Column name when RuleType=required-input." .

effortless-ntwf:requirementStatement a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:IngestionProtocol ;
    rdfs:range xsd:string ;
    rdfs:label "RequirementStatement" ;
    rdfs:comment "Plain-English statement of the requirement." .

effortless-ntwf:mechanicalCheck a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "MechanicalCheck" ;
    rdfs:comment "How to verify compliance before classification." .

effortless-ntwf:sortOrder a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "SortOrder" ;
    rdfs:comment "Display order in the ingestion checklist." .

# === Class: IngestionSummary ===
effortless-ntwf:IngestionSummary a owl:Class ;
    rdfs:label "IngestionSummary" ;
    rdfs:comment "Table: IngestionSummary — witnesses that the current corpus satisfies the IngestionProtocol contract." .

effortless-ntwf:ingestionSummaryId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:IngestionSummary ;
    rdfs:range xsd:string ;
    rdfs:label "IngestionSummaryId" ;
    rdfs:comment "Unique identifier." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:protocolItemCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:IngestionSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "ProtocolItemCount" ;
    rdfs:comment "Total contract items. Fixed loop-90: COUNTIFS(...,\"<>\") mistranslates to a literal '<>' string-equality filter (a known transpiler bug) instead of IS NOT NULL, always returning 0; replaced with bare COUNT(), which transpiles correctly." .
    # calculated: aggregation

effortless-ntwf:corpusCellCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:IngestionSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "CorpusCellCount" ;
    rdfs:comment "Total CaseCell rows. Fixed loop-90 (see ProtocolItemCount)." .
    # calculated: aggregation

effortless-ntwf:validCellCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:IngestionSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "ValidCellCount" ;
    rdfs:comment "Valid CaseCells." .
    # calculated: aggregation

effortless-ntwf:allCellsValid a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:IngestionSummary ;
    rdfs:range xsd:boolean ;
    rdfs:label "AllCellsValid" ;
    rdfs:comment "Every cell passes validity predicate." .
    # calculated: calculated

effortless-ntwf:studyCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "StudyCount" ;
    rdfs:comment "Count of studies subject to the ingestion contract (excludes control studies, loop-92 -- control studies were never encoded with paired treatment arms, so IngestionCellParity is structurally undefined for them, not a real ingestion defect)." .
    # calculated: aggregation

effortless-ntwf:structuralCompliantCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:IngestionSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "StructuralCompliantCount" ;
    rdfs:comment "Studies with IngestionCellParity." .
    # calculated: aggregation

effortless-ntwf:allStudiesStructural a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:IngestionSummary ;
    rdfs:range xsd:boolean ;
    rdfs:label "AllStudiesStructural" ;
    rdfs:comment "All studies structurally valid." .
    # calculated: calculated

effortless-ntwf:realStudyCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "RealStudyCount" ;
    rdfs:comment "Non-synthetic, non-control studies (loop-92 -- control studies were never encoded with paired treatment arms, so they are outside the ingestion contract's scope, same as StructuralCompliantCount's StudyCount)." .
    # calculated: aggregation

effortless-ntwf:realFullyCompliantCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:IngestionSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "RealFullyCompliantCount" ;
    rdfs:comment "Real studies with IngestionCompliance=all." .
    # calculated: aggregation

effortless-ntwf:allRealStudiesCompliant a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:IngestionSummary ;
    rdfs:range xsd:boolean ;
    rdfs:label "AllRealStudiesCompliant" ;
    rdfs:comment "All real studies fully compliant." .
    # calculated: calculated

effortless-ntwf:ingestionContractPasses a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:IngestionSummary ;
    rdfs:range xsd:boolean ;
    rdfs:label "IngestionContractPasses" ;
    rdfs:comment "Full contract passes." .
    # calculated: calculated

effortless-ntwf:ingestionWitnessNote a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "IngestionWitnessNote" ;
    rdfs:comment "PASS/FAIL summary." .
    # calculated: calculated

# === Class: InstrumentSpec ===
effortless-ntwf:InstrumentSpec a owl:Class ;
    rdfs:label "InstrumentSpec" ;
    rdfs:comment "Table: InstrumentSpec — the complete specification of the Simpson's Paradox classification instrument. One row per component: required input fields, derived coordinates, classification outputs, and adapter requirements. Every row is machine-verifiable against the 238-study corpus. This table is the distillation of loops 1–25 into a reusable tool." .

effortless-ntwf:specId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:InstrumentSpec ;
    rdfs:range xsd:string ;
    rdfs:label "SpecId" ;
    rdfs:comment "Unique identifier for this instrument component." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:componentType a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:InstrumentSpec ;
    rdfs:range xsd:string ;
    rdfs:label "ComponentType" ;
    rdfs:comment "input | derived | classification | adapter-requirement" .

effortless-ntwf:fieldName a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "FieldName" ;
    rdfs:comment "The field or variable name in the DAG." .

effortless-ntwf:dataType a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "DataType" ;
    rdfs:comment "integer | decimal | boolean | string | categorical" .

effortless-ntwf:sourceTable a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SourceTable" ;
    rdfs:comment "The DAG table where this field is computed or stored." .

effortless-ntwf:formula a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:InstrumentSpec ;
    rdfs:range xsd:string ;
    rdfs:label "Formula" ;
    rdfs:comment "The ERB formula or derivation rule for this component." .

effortless-ntwf:naturalLanguage a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "NaturalLanguage" ;
    rdfs:comment "Plain-English statement of what this component represents or requires." .

effortless-ntwf:adapterNote a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:InstrumentSpec ;
    rdfs:range xsd:string ;
    rdfs:label "AdapterNote" ;
    rdfs:comment "What a researcher must supply or verify to satisfy this component when hydrating a new study." .

# === Class: InvariantChecks ===
effortless-ntwf:InvariantChecks a owl:Class ;
    rdfs:label "InvariantChecks" ;
    rdfs:comment "Table: InvariantChecks — one row per algebraic invariant across the 40-study validation corpus. PassCount and FailCount are populated by run_invariant_checks() in 05b-customize-data.sql. Any critical FailCount > 0 breaks the build." .

effortless-ntwf:invariantCheckId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:InvariantChecks ;
    rdfs:range xsd:string ;
    rdfs:label "InvariantCheckId" ;
    rdfs:comment "Unique slug for this invariant (e.g. inv-balanced-alloc)." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:algebraicStatement a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:InvariantChecks ;
    rdfs:range xsd:string ;
    rdfs:label "AlgebraicStatement" ;
    rdfs:comment "The mathematical assertion in compact symbolic form." .

effortless-ntwf:naturalLanguage a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "NaturalLanguage" ;
    rdfs:comment "Plain-English statement of what property must hold and why it is an invariant of the model." .

effortless-ntwf:sourceTable a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SourceTable" ;
    rdfs:comment "The DAG table whose rows are checked (TreatmentRankings or StratumSummaries)." .

effortless-ntwf:filterExpression a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:InvariantChecks ;
    rdfs:range xsd:string ;
    rdfs:label "FilterExpression" ;
    rdfs:comment "Condition selecting the universe of rows this invariant applies to. Empty means all rows." .

effortless-ntwf:assertionExpression a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:InvariantChecks ;
    rdfs:range xsd:string ;
    rdfs:label "AssertionExpression" ;
    rdfs:comment "The per-row condition that must be TRUE for every row in scope." .

effortless-ntwf:sqlFilter a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:InvariantChecks ;
    rdfs:range xsd:string ;
    rdfs:label "SqlFilter" ;
    rdfs:comment "Optional SQL WHERE clause that selects the universe of rows in scope for this invariant (e.g. distortion_type = 'D'). Empty string means all rows." .

effortless-ntwf:sqlAssertion a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:InvariantChecks ;
    rdfs:range xsd:string ;
    rdfs:label "SqlAssertion" ;
    rdfs:comment "The SQL WHERE clause (without WHERE keyword) that counts PASSING rows from vw_treatment_rankings for this invariant. Used by run_invariant_checks() to evaluate each assertion dynamically from the rulebook table." .

effortless-ntwf:passCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:InvariantChecks ;
    rdfs:range xsd:integer ;
    rdfs:label "PassCount" ;
    rdfs:comment "Rows where the invariant holds. Populated by 05b-customize-data.sql from a live query against vw_treatment_rankings." .

effortless-ntwf:failCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:InvariantChecks ;
    rdfs:range xsd:integer ;
    rdfs:label "FailCount" ;
    rdfs:comment "Rows where the invariant is violated. Any FailCount > 0 is a build-breaking bug." .

effortless-ntwf:universeCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:InvariantChecks ;
    rdfs:range xsd:integer ;
    rdfs:label "UniverseCount" ;
    rdfs:comment "Total rows in scope (PassCount + FailCount)." .
    # calculated: calculated

effortless-ntwf:isGreen a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:InvariantChecks ;
    rdfs:range xsd:boolean ;
    rdfs:label "IsGreen" ;
    rdfs:comment "TRUE when FailCount = 0." .
    # calculated: calculated

effortless-ntwf:statusLabel a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:InvariantChecks ;
    rdfs:range xsd:string ;
    rdfs:label "StatusLabel" ;
    rdfs:comment "PASS or FAIL(n) summary for display." .
    # calculated: calculated

effortless-ntwf:severity a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:InvariantChecks ;
    rdfs:range xsd:string ;
    rdfs:label "Severity" ;
    rdfs:comment "critical | warning — critical means any FailCount > 0 is a build-breaking bug." .

effortless-ntwf:traditionId a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionId" ;
    rdfs:comment "FK → ResearchTraditions.TraditionId. The tradition whose theoretical commitments this algebraic invariant protects — a failed invariant would falsify that tradition's key claim." .

effortless-ntwf:protectsConclusion a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:InvariantChecks ;
    rdfs:range xsd:string ;
    rdfs:label "ProtectsConclusion" ;
    rdfs:comment "FK → Conclusions.ConclusionId. The conclusion whose witness depends on this invariant holding. If this invariant fails, that conclusion is undermined." .

effortless-ntwf:traditionName a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionName" ;
    rdfs:comment "Lookup: ResearchTraditions.Name via TraditionId." .
    # calculated: lookup

effortless-ntwf:traditionKeyClaimSummary a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionKeyClaimSummary" ;
    rdfs:comment "Lookup: ResearchTraditions.KeyClaim via TraditionId — a failed invariant would falsify this tradition's key claim." .
    # calculated: lookup

effortless-ntwf:protectsConclusionTitle a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:InvariantChecks ;
    rdfs:range xsd:string ;
    rdfs:label "ProtectsConclusionTitle" ;
    rdfs:comment "Lookup: Conclusions.Title via ProtectsConclusion." .
    # calculated: lookup

effortless-ntwf:protectsConclusionCategory a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:InvariantChecks ;
    rdfs:range xsd:string ;
    rdfs:label "ProtectsConclusionCategory" ;
    rdfs:comment "Lookup: Conclusions.Category via ProtectsConclusion." .
    # calculated: lookup

# === Class: Loops ===
effortless-ntwf:Loops a owl:Class ;
    rdfs:label "Loops" ;
    rdfs:comment "Table: Loops — build history and forward plan, carried as a first-class entity inside the model. Each row documents what new concept is introduced, what was witnessed, and (for planned rows) the next empirical question. Loops 62–67 are the corpus-scale research sweep + catalog expansion. Loops 68+ are the expansion encode waves: pre-register in DiscoveryHypotheses, encode via StudyImportTemplate, LoopReview at end of each turn before committing vocabulary." .

effortless-ntwf:loopId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Loops ;
    rdfs:range xsd:string ;
    rdfs:label "LoopId" ;
    rdfs:comment "Unique identifier: loop-01 through loop-10." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:title a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Title" ;
    rdfs:comment "Short title for what this loop adds to the domain model." .

effortless-ntwf:status a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Status" ;
    rdfs:comment "planned | in-progress | complete" .

effortless-ntwf:newConcept a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Loops ;
    rdfs:range xsd:string ;
    rdfs:label "NewConcept" ;
    rdfs:comment "The new first-class domain concept introduced this loop." .

effortless-ntwf:domainQuestion a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Loops ;
    rdfs:range xsd:string ;
    rdfs:label "DomainQuestion" ;
    rdfs:comment "The natural-language question this loop's concept answers FROM the domain data." .

effortless-ntwf:mockDataNote a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Loops ;
    rdfs:range xsd:string ;
    rdfs:label "MockDataNote" ;
    rdfs:comment "What the seed data is designed to show." .

effortless-ntwf:nextSuggestion a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Loops ;
    rdfs:range xsd:string ;
    rdfs:label "NextSuggestion" ;
    rdfs:comment "Suggestion for the next loop, written after witnessing the DAG at this loop's end." .

effortless-ntwf:traditionId a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionId" ;
    rdfs:comment "FK → ResearchTraditions.TraditionId. The scholarly tradition whose conceptual vocabulary this loop primarily draws on — tracks which tradition has driven each stage of the instrument's development." .

effortless-ntwf:traditionName a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionName" ;
    rdfs:comment "Lookup: ResearchTraditions.Name via TraditionId." .
    # calculated: lookup

effortless-ntwf:traditionCoreConern a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionCoreConern" ;
    rdfs:comment "Lookup: ResearchTraditions.CoreConcern via TraditionId — which intellectual concern drove this loop." .
    # calculated: lookup

effortless-ntwf:commitHash a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Loops ;
    rdfs:range xsd:string ;
    rdfs:label "CommitHash" ;
    rdfs:comment "Full git SHA where this loop row first landed in effortless-rulebook.json — the replay anchor for this turn of the Effortless loop." .

effortless-ntwf:commitShort a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Loops ;
    rdfs:range xsd:string ;
    rdfs:label "CommitShort" ;
    rdfs:comment "First 7 characters of CommitHash for display and git checkout." .
    # calculated: calculated

effortless-ntwf:commitDate a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Loops ;
    rdfs:range xsd:string ;
    rdfs:label "CommitDate" ;
    rdfs:comment "ISO date (YYYY-MM-DD) of the commit where this loop landed." .

effortless-ntwf:commitMessage a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Loops ;
    rdfs:range xsd:string ;
    rdfs:label "CommitMessage" ;
    rdfs:comment "Git commit subject line for this loop's landing commit." .

effortless-ntwf:gitTag a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Loops ;
    rdfs:range xsd:string ;
    rdfs:label "GitTag" ;
    rdfs:comment "Optional milestone tag pointing at this loop's landing commit (sp-loop-milestone-*). Used for discovery replay without memorizing SHAs." .

# === Class: MaterializedEntities ===
effortless-ntwf:MaterializedEntities a owl:Class ;
    rdfs:label "MaterializedEntities" ;
    rdfs:comment "Table: MaterializedEntities — governance record of every entity in this rulebook whose fields are principled materializations rather than live-calculated (per project CLAUDE.md: allowed only when the computation is genuinely expensive AND its upstream is sealed/stable, expressed as data in the rulebook itself, never as an ad-hoc application cache). Each row names the entity, the refresh contract, the refresh function that implements it, and the measured cost that justified materializing it. If this table's row for an entity were deleted, the entity's fields should still be regeneratable identically from the SSoT + the refresh function — that is the test for whether a materialization here is principled." .

effortless-ntwf:materializedEntityId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:MaterializedEntities ;
    rdfs:range xsd:string ;
    rdfs:label "MaterializedEntityId" ;
    rdfs:comment "Unique slug (e.g. mat-sweep-study-summary)." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label, mirrors MaterializedEntityId." .
    # calculated: calculated

effortless-ntwf:entityTable a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:MaterializedEntities ;
    rdfs:range xsd:string ;
    rdfs:label "EntityTable" ;
    rdfs:comment "The rulebook table whose fields are materialized (e.g. SweepStudySummary)." .

effortless-ntwf:refreshContract a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:MaterializedEntities ;
    rdfs:range xsd:string ;
    rdfs:label "RefreshContract" ;
    rdfs:comment "eager-on-write | on-demand | scheduled — when the refresh function is invoked relative to writes on the upstream data." .

effortless-ntwf:refreshFunction a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:MaterializedEntities ;
    rdfs:range xsd:string ;
    rdfs:label "RefreshFunction" ;
    rdfs:comment "The SQL function (in the customize-* override files) that recomputes this entity's materialized fields from its upstream SSoT tables." .

effortless-ntwf:upstreamTables a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:MaterializedEntities ;
    rdfs:range xsd:string ;
    rdfs:label "UpstreamTables" ;
    rdfs:comment "Comma-separated list of the tables this materialization is derived from — its sealed/stable upstream." .

effortless-ntwf:justification a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:MaterializedEntities ;
    rdfs:range xsd:string ;
    rdfs:label "Justification" ;
    rdfs:comment "The measured cost or evidence that justified materializing this entity instead of leaving it as a live calculated/aggregation field." .

# === Class: Methodology ===
effortless-ntwf:Methodology a owl:Class ;
    rdfs:label "Methodology" ;
    rdfs:comment "Table: Methodology — the first-principles process used to build this domain model. Each row documents one methodological axiom or step. Together these rows let a reader (or an LLM) reconstruct why the model is shaped the way it is, and re-apply the same process to a new domain." .

effortless-ntwf:methodologyId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Methodology ;
    rdfs:range xsd:string ;
    rdfs:label "MethodologyId" ;
    rdfs:comment "Unique slug (e.g. method-01-cmcc, method-02-dag-first)." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label, mirrors MethodologyId." .
    # calculated: calculated

effortless-ntwf:phase a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Methodology ;
    rdfs:range xsd:string ;
    rdfs:label "Phase" ;
    rdfs:comment "Broad phase: axiom | decomposition | derivation | hydration | validation | taxonomy | conclusion" .

effortless-ntwf:title a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Title" ;
    rdfs:comment "Short title of the methodological principle or step." .

effortless-ntwf:statement a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Statement" ;
    rdfs:comment "One-to-three sentence statement of the principle, precise enough to be applied or falsified." .

effortless-ntwf:whyItMatters a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Methodology ;
    rdfs:range xsd:string ;
    rdfs:label "WhyItMatters" ;
    rdfs:comment "Why this step or principle is load-bearing for this model — what breaks if you skip it." .

effortless-ntwf:illustratedByLoop a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Methodology ;
    rdfs:range xsd:string ;
    rdfs:label "IllustratedByLoop" ;
    rdfs:comment "LoopId(s) where this principle was concretely demonstrated (comma-separated)." .

effortless-ntwf:traditionId a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionId" ;
    rdfs:comment "FK → ResearchTraditions.TraditionId. The scholarly tradition that introduced or most relies on this methodological approach." .

effortless-ntwf:pioneeringResearcher a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Methodology ;
    rdfs:range xsd:string ;
    rdfs:label "PioneeringResearcher" ;
    rdfs:comment "FK → Researchers.ResearcherId. The researcher most associated with developing or popularising this methodological principle in the Simpson's Paradox context." .

effortless-ntwf:traditionName a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionName" ;
    rdfs:comment "Lookup: ResearchTraditions.Name via TraditionId." .
    # calculated: lookup

effortless-ntwf:traditionPrimaryVenue a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Methodology ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionPrimaryVenue" ;
    rdfs:comment "Lookup: ResearchTraditions.PrimaryVenue via TraditionId." .
    # calculated: lookup

effortless-ntwf:pioneeringResearcherName a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Methodology ;
    rdfs:range xsd:string ;
    rdfs:label "PioneeringResearcherName" ;
    rdfs:comment "Lookup: Researchers.Name via PioneeringResearcher." .
    # calculated: lookup

effortless-ntwf:pioneeringResearcherAffiliation a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Methodology ;
    rdfs:range xsd:string ;
    rdfs:label "PioneeringResearcherAffiliation" ;
    rdfs:comment "Lookup: Researchers.Affiliation via PioneeringResearcher." .
    # calculated: lookup

effortless-ntwf:loopCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "LoopCount" ;
    rdfs:comment "Count of Loops whose TraditionId matches this methodology entry's TraditionId — how many build iterations this methodological tradition has driven." .
    # calculated: aggregation

# === Class: ModelSummary ===
effortless-ntwf:ModelSummary a owl:Class ;
    rdfs:label "ModelSummary" ;
    rdfs:comment "Table: ModelSummary — one row describing the epistemic coverage of this entire model instance. Aggregates across all TreatmentRankings to witness how many studies were examined, how many showed a full reversal, how many were explained, and the average paradox strength. The model holds a mirror to itself." .

effortless-ntwf:modelSummaryId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "ModelSummaryId" ;
    rdfs:comment "Unique identifier for this model instance." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:reversalCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "ReversalCount" ;
    rdfs:comment "Number of studies with IsReversal=TRUE (sign-flip / allocation-driven reversal)." .
    # calculated: aggregation

effortless-ntwf:nonReversalCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "NonReversalCount" ;
    rdfs:comment "Number of studies with IsReversal=FALSE." .
    # calculated: aggregation

effortless-ntwf:studyCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "StudyCount" ;
    rdfs:comment "Total number of TreatmentRankings in this model: ReversalCount + NonReversalCount." .
    # calculated: calculated

effortless-ntwf:explainedCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "ExplainedCount" ;
    rdfs:comment "Number of studies with IsParadoxExplained=TRUE (reversal AND confirmed confounder)." .
    # calculated: aggregation

effortless-ntwf:totalParadoxStrength a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "TotalParadoxStrength" ;
    rdfs:comment "Sum of ParadoxStrength across all studies. Used to compute average." .
    # calculated: aggregation

effortless-ntwf:avgParadoxStrength a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "AvgParadoxStrength" ;
    rdfs:comment "Average ParadoxStrength across all studies: TotalParadoxStrength / StudyCount. A scalar summary of how paradox-rich this dataset is." .
    # calculated: calculated

effortless-ntwf:typeACount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "TypeACount" ;
    rdfs:comment "Studies classified as Type-A: sign flip AND full reversal (ReversalIntensity=1). The canonical Simpson's Paradox — allocation distortion reverses the pooled winner." .
    # calculated: aggregation

effortless-ntwf:dangerTierCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "DangerTierCount" ;
    rdfs:comment "Studies where TreatmentRankings.ScreeningTier = DANGER — pooled sign is wrong (DistortionType A or B)." .
    # calculated: aggregation

effortless-ntwf:cautionTierCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "CautionTierCount" ;
    rdfs:comment "Studies where TreatmentRankings.ScreeningTier = CAUTION — pooled sign holds but magnitude is distorted (DistortionType C+ or C-)." .
    # calculated: aggregation

effortless-ntwf:safeTierCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "SafeTierCount" ;
    rdfs:comment "Studies where TreatmentRankings.ScreeningTier = SAFE — pooling is trustworthy (DistortionType D)." .
    # calculated: aggregation

effortless-ntwf:typeBCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "TypeBCount" ;
    rdfs:comment "Studies classified as Type-B: sign flip but partial reversal (ReversalIntensity<1). Allocation distortion creates a sign flip but the pooled signal partially reflects per-stratum reality." .
    # calculated: aggregation

effortless-ntwf:typeDCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "TypeDCount" ;
    rdfs:comment "Studies classified as Type-D: no sign flip, negligible distortion. Pooled analysis is trustworthy — allocation is sufficiently balanced." .
    # calculated: aggregation

effortless-ntwf:typeCPlusCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "TypeCPlusCount" ;
    rdfs:comment "Studies classified as Type-C+: sign flip with amplification (ReversalIntensity>1). Pooled effect overstates the true per-stratum effect." .
    # calculated: aggregation

effortless-ntwf:typeCMinusCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "TypeCMinusCount" ;
    rdfs:comment "Studies classified as Type-C-: sign flip with attenuation. Pooled effect understates or masks the true per-stratum effect." .
    # calculated: aggregation

effortless-ntwf:typeAFraction a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "TypeAFraction" ;
    rdfs:comment "Fraction of studies that are Type-A (full canonical reversals). TypeACount / StudyCount." .
    # calculated: calculated

effortless-ntwf:distortionTaxonomyCoverage a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "DistortionTaxonomyCoverage" ;
    rdfs:comment "Human-readable summary of the four-type distribution: e.g. 'A:2 B:1 C:1 D:0'. The model's self-portrait of its own distortion geometry." .
    # calculated: calculated

effortless-ntwf:distortionOnlyCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "DistortionOnlyCount" ;
    rdfs:comment "Number of studies with real allocation distortion but no sign flip: TypeCCount. The pooled winner is correct but the effect size is biased. Distinct from ZeroStrengthCount, which conflates Type-C (real distortion) with Type-D (neutral)." .
    # calculated: calculated

effortless-ntwf:cAmplificationCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "CAmplificationCount" ;
    rdfs:comment "Number of studies classified as type C+ (allocation-amplified): pooled winner is correct but pooled gap overstates true advantage. Causes researcher overconfidence — may accept higher cost/risk than true effect warrants." .
    # calculated: aggregation

effortless-ntwf:cCompressionCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "CCompressionCount" ;
    rdfs:comment "Number of studies classified as type C- (allocation-compressed): pooled winner is correct but pooled gap understates true advantage. Causes researcher underconfidence — effective treatment may be prematurely abandoned." .
    # calculated: aggregation

effortless-ntwf:avgSignalPurity a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "AvgSignalPurity" ;
    rdfs:comment "Average SignalPurity across all TreatmentRankings. Below 0.5 means allocation noise exceeds true signal on average — the corpus is predominantly misleading at the pooled level. Above 0.5 means the corpus is predominantly trustworthy. Fixed loop-90: COUNTIFS(...,\"<>\")/SUMIFS(...,\"<>\") mistranslate to a literal '<>' string-equality filter (a known transpiler bug), always returning 0/NULL; replaced with SUM/COUNT, both of which transpile correctly." .
    # calculated: aggregation

effortless-ntwf:sweepCorrectedGapMax a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SweepCorrectedGapMax" ;
    rdfs:comment "Maximum SweepCorrectedGap across all AllocationSweep rows. Should equal SweepCorrectedGapMin — the invariant." .
    # calculated: aggregation

effortless-ntwf:sweepCorrectedGapMin a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SweepCorrectedGapMin" ;
    rdfs:comment "Minimum SweepCorrectedGap across all AllocationSweep rows. Should equal SweepCorrectedGapMax — the invariant." .
    # calculated: aggregation

effortless-ntwf:sweepCorrectedGapRange a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SweepCorrectedGapRange" ;
    rdfs:comment "SweepCorrectedGapMax − SweepCorrectedGapMin. The allocation-invariance witness: this should be zero (up to floating-point rounding). If nonzero, the corrected gap is not allocation-free." .
    # calculated: calculated

effortless-ntwf:sweepPooledGapRange a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SweepPooledGapRange" ;
    rdfs:comment "MAX(SweepPooledGap) − MIN(SweepPooledGap) across all AllocationSweep rows. Expected to be large (≈0.21) — the pooled gap wanders as allocation shifts." .
    # calculated: aggregation

effortless-ntwf:realStudyCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "RealStudyCount" ;
    rdfs:comment "Number of real (non-synthetic) published studies in the corpus. Retyped loop-90: was mis-declared raw with a formula, which is why the transpiler emitted no native function and 02b had to hand-write calc_model_summary_real_study_count." .
    # calculated: aggregation

effortless-ntwf:avgSignalPurityReversal a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "AvgSignalPurityReversal" ;
    rdfs:comment "Average SignalPurity across reversal studies (type A and B). Theorem predicts this must be < 0.5. Retyped loop-90 (see RealStudyCount); rewritten from SUMIFS/COUNTIFS division to AVERAGEIFS because the SUMIFS/COUNTIFS division form transpiled with a mangled cross-table alias." .
    # calculated: calculated

effortless-ntwf:avgSignalPurityNonReversal a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "AvgSignalPurityNonReversal" ;
    rdfs:comment "Average SignalPurity across non-reversal studies (type C+, C-, D). Theorem predicts this must be >= 0.5 except edge cases. Retyped loop-90 (see RealStudyCount); rewritten from SUMIFS/COUNTIFS division to AVERAGEIFS (see AvgSignalPurityReversal)." .
    # calculated: calculated

effortless-ntwf:signalPurityGap a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "SignalPurityGap" ;
    rdfs:comment "Separation between reversal and non-reversal average SignalPurity. The larger this gap, the more discriminating the threshold at 0.5. Retyped loop-90 (see RealStudyCount)." .
    # calculated: calculated

effortless-ntwf:medicineStudyCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "MedicineStudyCount" ;
    rdfs:comment "Real published studies in the medicine domain. Retyped loop-90 (see RealStudyCount)." .
    # calculated: aggregation

effortless-ntwf:epidemiologyStudyCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "EpidemiologyStudyCount" ;
    rdfs:comment "Real published studies in the epidemiology domain. Retyped loop-90 (see RealStudyCount)." .
    # calculated: aggregation

effortless-ntwf:otherDomainStudyCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "OtherDomainStudyCount" ;
    rdfs:comment "Real published studies outside medicine and epidemiology (sports, legal, education, social-science). Retyped loop-90 (see RealStudyCount)." .
    # calculated: calculated

effortless-ntwf:domainDiversityNote a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "DomainDiversityNote" ;
    rdfs:comment "Human-readable domain breakdown. Witnesses corpus diversity — a prerequisite for cross-domain SignalPurity claims. Retyped loop-90 (see RealStudyCount); TEXT(...,\"0%\") is not a supported transpiler function, replaced with ROUND(x*100,0) + literal '%'." .
    # calculated: calculated

effortless-ntwf:syntheticPhaseCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "SyntheticPhaseCount" ;
    rdfs:comment "Number of parameter grid points in SyntheticPhase. Refreshed from vw_phase_diagram_summary by 05b-customize-data.sql." .

effortless-ntwf:phaseDiagramComplete a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:boolean ;
    rdfs:label "PhaseDiagramComplete" ;
    rdfs:comment "TRUE when all five DistortionType cells are populated in the synthetic phase grid. Refreshed from vw_phase_diagram_summary by 05b." .

effortless-ntwf:phaseTaxonomyCoverage a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseTaxonomyCoverage" ;
    rdfs:comment "Type counts across the synthetic phase grid. Refreshed from vw_phase_diagram_summary by 05b." .

effortless-ntwf:ingestionProtocolItemCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "IngestionProtocolItemCount" ;
    rdfs:comment "Number of rows in IngestionProtocol — the documented contract items. Fixed loop-90 (see IngestionSummary.ProtocolItemCount for why COUNTIFS(...,\"<>\") is unsafe)." .
    # calculated: aggregation

effortless-ntwf:corpusPassesIngestionContract a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:boolean ;
    rdfs:label "CorpusPassesIngestionContract" ;
    rdfs:comment "Lookup: IngestionSummary.IngestionContractPasses." .
    # calculated: lookup

effortless-ntwf:ingestionWitnessNote a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "IngestionWitnessNote" ;
    rdfs:comment "Human-readable ingestion contract witness from IngestionSummary." .
    # calculated: lookup

effortless-ntwf:catalogEntryCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "CatalogEntryCount" ;
    rdfs:comment "Total catalog rows." .
    # calculated: lookup

effortless-ntwf:pendingImportCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "PendingImportCount" ;
    rdfs:comment "Candidate studies queued." .
    # calculated: lookup

effortless-ntwf:readyToEncodeCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "ReadyToEncodeCount" ;
    rdfs:comment "Encode-ready candidates." .
    # calculated: lookup

effortless-ntwf:importSessionReady a owl:DatatypeProperty ;
    rdfs:range xsd:boolean ;
    rdfs:label "ImportSessionReady" ;
    rdfs:comment "Bulk import session readiness flag." .
    # calculated: lookup

effortless-ntwf:catalogWitnessNote a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "CatalogWitnessNote" ;
    rdfs:comment "Import backlog witness string." .
    # calculated: lookup

effortless-ntwf:latentTypeDCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "LatentTypeDCount" ;
    rdfs:comment "Type-D studies with LatentFlipPotential=TRUE." .
    # calculated: calculated

effortless-ntwf:stableTypeDCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "StableTypeDCount" ;
    rdfs:comment "Type-D studies without latent flip potential (allocation-stable at observed and counterfactual sweep)." .
    # calculated: calculated

effortless-ntwf:latentTypeDFraction a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "LatentTypeDFraction" ;
    rdfs:comment "Fraction of Type-D studies with latent flip potential." .
    # calculated: calculated

effortless-ntwf:crossZeroCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "CrossZeroCount" ;
    rdfs:comment "Studies whose pooled gap crosses zero under allocation sweep." .
    # calculated: calculated

effortless-ntwf:signFlipSignalPurityMax a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "SignFlipSignalPurityMax" ;
    rdfs:comment "Maximum SignalPurity among sign-flip studies. Must stay below 0.5 for the signal-purity theorem." .
    # calculated: calculated

effortless-ntwf:economicsSignFlipCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "EconomicsSignFlipCount" ;
    rdfs:comment "Sign-flip count in economics domain." .
    # calculated: calculated

effortless-ntwf:sumPooledGapLatentD a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "SumPooledGapLatentD" ;
    rdfs:comment "SUM(PooledGap) among Type-D studies with latent flip potential — a bare, unwrapped SUMIFS split out as its own field because nesting a cross-table SUMIFS/AVERAGEIFS as an operand inside another IF/division expression transpiles with a mangled WHERE-clause column alias (e.g. rankings_latent_flip_potential) that errors at runtime; a bare top-level SUMIFS transpiles correctly (loop-90)." .
    # calculated: aggregation

effortless-ntwf:avgPooledGapLatentD a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "AvgPooledGapLatentD" ;
    rdfs:comment "Average PooledGap among Type-D studies with latent flip potential. Fixed loop-90 (see SumPooledGapLatentD for why the sum is split into its own field)." .
    # calculated: calculated

effortless-ntwf:sumPooledGapStableD a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "SumPooledGapStableD" ;
    rdfs:comment "SUM(PooledGap) among allocation-stable Type-D studies — split out as a bare field for the same reason as SumPooledGapLatentD." .
    # calculated: aggregation

effortless-ntwf:avgPooledGapStableD a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "AvgPooledGapStableD" ;
    rdfs:comment "Average PooledGap among allocation-stable Type-D studies. Fixed loop-90 (see SumPooledGapLatentD)." .
    # calculated: calculated

effortless-ntwf:epidemiologyAvgDistortion a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "EpidemiologyAvgDistortion" ;
    rdfs:comment "Mean AllocationDistortion in epidemiology studies." .
    # calculated: calculated

effortless-ntwf:educationAvgDistortion a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "EducationAvgDistortion" ;
    rdfs:comment "Mean AllocationDistortion in education studies." .
    # calculated: calculated

effortless-ntwf:confounderSignFlipCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "ConfounderSignFlipCount" ;
    rdfs:comment "Sign-flip count among confounder-labeled studies." .
    # calculated: calculated

effortless-ntwf:confounderLatentOnlyCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "ConfounderLatentOnlyCount" ;
    rdfs:comment "Latent-only flip count among confounder-labeled studies." .
    # calculated: calculated

effortless-ntwf:colliderSelectionCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "ColliderSelectionCount" ;
    rdfs:comment "Studies tagged collider or selection on StratumVariables." .
    # calculated: calculated

effortless-ntwf:colliderSelectionManifestCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "ColliderSelectionManifestCount" ;
    rdfs:comment "Manifest sign-flips among collider/selection studies." .
    # calculated: calculated

effortless-ntwf:colliderSelectionLatentOnlyCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "ColliderSelectionLatentOnlyCount" ;
    rdfs:comment "Latent-only flips among collider/selection studies." .
    # calculated: calculated

effortless-ntwf:explainedConfounderCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "ExplainedConfounderCount" ;
    rdfs:comment "Count of TreatmentRankings rows where IsParadoxExplained=TRUE (should equal ConfounderSignFlipCount)." .
    # calculated: aggregation

effortless-ntwf:contestedOrMediatorExplainedCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "ContestedOrMediatorExplainedCount" ;
    rdfs:comment "Count of contested or mediator sign-flip rows where IsParadoxExplained=TRUE (should be zero)." .
    # calculated: aggregation

effortless-ntwf:discoveryWitnessNote a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "DiscoveryWitnessNote" ;
    rdfs:comment "Research sweep synthesis rollup (loops 61–66)." .
    # calculated: calculated

effortless-ntwf:typePredictionMatchCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "TypePredictionMatchCount" ;
    rdfs:comment "Lookup: CorpusCatalogSummary.TypePredictionMatchCount." .
    # calculated: lookup

effortless-ntwf:typePredictionMismatchCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "TypePredictionMismatchCount" ;
    rdfs:comment "Lookup: CorpusCatalogSummary.TypePredictionMismatchCount." .
    # calculated: lookup

effortless-ntwf:typePredictionMatchRate a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TypePredictionMatchRate" ;
    rdfs:comment "Lookup: CorpusCatalogSummary.TypePredictionMatchRate." .
    # calculated: lookup

effortless-ntwf:signFlipPredictionMatchRate a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SignFlipPredictionMatchRate" ;
    rdfs:comment "Lookup: CorpusCatalogSummary.SignFlipPredictionMatchRate." .
    # calculated: lookup

effortless-ntwf:catalogPredictionWitnessNote a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "CatalogPredictionWitnessNote" ;
    rdfs:comment "Lookup: CorpusCatalogSummary.CatalogPredictionWitnessNote." .
    # calculated: lookup

effortless-ntwf:highImbalanceSignFlipThreshold a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "HighImbalanceSignFlipThreshold" ;
    rdfs:comment "max_stratum_imbalance cutoff for geometry-controlled domain screening (loop-66). Set from corpus median by customize SQL." .

effortless-ntwf:economicsHighImbalanceSignFlipCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "EconomicsHighImbalanceSignFlipCount" ;
    rdfs:comment "Sign-flip count among economics studies with max_stratum_imbalance >= threshold." .

effortless-ntwf:epidemiologyHighImbalanceSignFlipRate a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "EpidemiologyHighImbalanceSignFlipRate" ;
    rdfs:comment "Sign-flip rate among high-imbalance epidemiology studies." .

effortless-ntwf:legalHighImbalanceSignFlipRate a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "LegalHighImbalanceSignFlipRate" ;
    rdfs:comment "Sign-flip rate among high-imbalance legal studies." .

effortless-ntwf:sportsHighImbalanceSignFlipRate a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "SportsHighImbalanceSignFlipRate" ;
    rdfs:comment "Sign-flip rate among high-imbalance sports studies." .

effortless-ntwf:cPlusAvgDistortion a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "CPlusAvgDistortion" ;
    rdfs:comment "Mean AllocationDistortion among Type-C+ studies." .
    # calculated: calculated

effortless-ntwf:cMinusAvgDistortion a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "CMinusAvgDistortion" ;
    rdfs:comment "Mean AllocationDistortion among Type-C- studies." .
    # calculated: calculated

effortless-ntwf:typeDAvgDistortion a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "TypeDAvgDistortion" ;
    rdfs:comment "Mean AllocationDistortion among Type-D studies." .
    # calculated: calculated

effortless-ntwf:sweepFragileCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "SweepFragileCount" ;
    rdfs:comment "Count of TreatmentRankings with IsSweepFragile=TRUE." .
    # calculated: calculated

effortless-ntwf:expansionWave1EconomicsExpectedACount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "ExpansionWave1EconomicsExpectedACount" ;
    rdfs:comment "Imported expansion-wave-1 economics catalog rows tagged Expected A." .
    # calculated: calculated

effortless-ntwf:expansionWave1EconomicsExpectedADCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "ExpansionWave1EconomicsExpectedADCount" ;
    rdfs:comment "Expansion-wave-1 economics Expected-A imports observed as Type D." .
    # calculated: calculated

effortless-ntwf:economicsExpectedAMismatchRate a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "EconomicsExpectedAMismatchRate" ;
    rdfs:comment "Fraction of expansion-wave-1 economics Expected-A imports observed as Type D." .
    # calculated: calculated

effortless-ntwf:unanimousSignFlipCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "UnanimousSignFlipCount" ;
    rdfs:comment "Sign-flip studies with unanimous stratum direction (IsSignFlip AND IsStratumUnanimous)." .
    # calculated: calculated

effortless-ntwf:maxStudySweepCorrectedGapRange a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "MaxStudySweepCorrectedGapRange" ;
    rdfs:comment "MAX(SweepStudySummary.SweepCorrectedGapRange) — must stay below 0.0001 for CorrectedGap invariance theorem." .
    # calculated: aggregation

effortless-ntwf:correctedGapInvariantFailCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "CorrectedGapInvariantFailCount" ;
    rdfs:comment "Count of SweepStudySummary rows where SweepCorrectedGapRange >= 0.0001." .
    # calculated: aggregation

effortless-ntwf:falsePositiveExplainedCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "FalsePositiveExplainedCount" ;
    rdfs:comment "IsParadoxExplained=TRUE rows that are not confounder manifest sign-flips — must be 0 for explained↔confounder theorem." .
    # calculated: aggregation

effortless-ntwf:unexplainedConfounderSignFlipCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "UnexplainedConfounderSignFlipCount" ;
    rdfs:comment "Confounder sign-flips with IsParadoxExplained=FALSE — must be 0 for explained↔confounder theorem." .
    # calculated: aggregation

effortless-ntwf:theoremCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "TheoremCount" ;
    rdfs:comment "Count of witnessed Conclusions with Category=corpus-theorem (portfolio rollup for loop-76; category renamed from theorem in loop-91)." .
    # calculated: calculated

effortless-ntwf:educationLatentFraction a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "EducationLatentFraction" ;
    rdfs:comment "Fraction of education Type-D studies with LatentFlipPotential=TRUE." .
    # calculated: calculated

effortless-ntwf:sportsLatentFraction a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "SportsLatentFraction" ;
    rdfs:comment "Fraction of sports Type-D studies with LatentFlipPotential=TRUE." .
    # calculated: calculated

effortless-ntwf:economicsSignFlipRate a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "EconomicsSignFlipRate" ;
    rdfs:comment "Manifest sign-flip rate among economics studies." .
    # calculated: calculated

effortless-ntwf:educationTypeDCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "EducationTypeDCount" ;
    rdfs:comment "Type-D studies in education domain." .
    # calculated: calculated

effortless-ntwf:educationLatentDCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "EducationLatentDCount" ;
    rdfs:comment "Education Type-D studies with LatentFlipPotential." .
    # calculated: calculated

effortless-ntwf:sportsTypeDCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "SportsTypeDCount" ;
    rdfs:comment "Type-D studies in sports domain." .
    # calculated: calculated

effortless-ntwf:sportsLatentDCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "SportsLatentDCount" ;
    rdfs:comment "Sports Type-D studies with LatentFlipPotential." .
    # calculated: calculated

effortless-ntwf:economicsStudyCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "EconomicsStudyCount" ;
    rdfs:comment "Real economics studies in corpus." .
    # calculated: calculated

effortless-ntwf:expansionWave2StudyCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "ExpansionWave2StudyCount" ;
    rdfs:comment "Studies imported in expansion wave 2." .
    # calculated: calculated

effortless-ntwf:confounderIdentityCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "ConfounderIdentityCount" ;
    rdfs:comment "Canonical ConfounderIdentity archetypes in ontology (loop-80)." .
    # calculated: aggregation

effortless-ntwf:mappedStratumVariableCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "MappedStratumVariableCount" ;
    rdfs:comment "Stratum variables with an identity map row." .
    # calculated: aggregation

effortless-ntwf:unmappedStratumVariableCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "UnmappedStratumVariableCount" ;
    rdfs:comment "Stratum variables lacking identity map (should be 0)." .
    # calculated: calculated

effortless-ntwf:ageIdentityManifestFlipRate a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "AgeIdentityManifestFlipRate" ;
    rdfs:comment "Manifest flip rate for id-age-composition cluster (loop-81)." .
    # calculated: calculated

effortless-ntwf:ageIdentityStudyCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "AgeIdentityStudyCount" ;
    rdfs:comment "Real-study count in id-age-composition cluster (loop-81)." .
    # calculated: calculated

effortless-ntwf:ageIdentityLatentFractionAmongTypeD a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "AgeIdentityLatentFractionAmongTypeD" ;
    rdfs:comment "Type-D latent fraction for id-age-composition cluster (loop-81)." .
    # calculated: calculated

effortless-ntwf:severityIdentityLatentFractionAmongTypeD a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "SeverityIdentityLatentFractionAmongTypeD" ;
    rdfs:comment "Type-D latent fraction for id-disease-severity cluster (loop-81)." .
    # calculated: calculated

effortless-ntwf:identityMapCoverageRate a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "IdentityMapCoverageRate" ;
    rdfs:comment "Fraction of real-study StratumVariables with a ConfounderIdentity map (loop-81 customize SQL)." .
    # calculated: calculated

effortless-ntwf:identityClusterWitnessNote a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "IdentityClusterWitnessNote" ;
    rdfs:comment "Loop-80 identity layer witness rollup (customize SQL)." .

effortless-ntwf:severityMedicineManifestFlipRate a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "SeverityMedicineManifestFlipRate" ;
    rdfs:comment "Severity identity manifest flip rate in medicine domain." .
    # calculated: calculated

effortless-ntwf:severityEpiManifestFlipCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "SeverityEpiManifestFlipCount" ;
    rdfs:comment "Severity identity manifest flip count in epidemiology domain." .
    # calculated: calculated

effortless-ntwf:identityDomainCellCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "IdentityDomainCellCount" ;
    rdfs:comment "Number of identity×domain cells." .
    # calculated: aggregation

effortless-ntwf:selectionFrailtyManifestFlipCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "SelectionFrailtyManifestFlipCount" ;
    rdfs:comment "Manifest flip count for selection-frailty identity." .
    # calculated: calculated

effortless-ntwf:selectionFrailtyStudyCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "SelectionFrailtyStudyCount" ;
    rdfs:comment "Study count for selection-frailty identity cluster." .
    # calculated: calculated

effortless-ntwf:colliderIdentityManifestFlipRate a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "ColliderIdentityManifestFlipRate" ;
    rdfs:comment "Manifest flip rate for collider-proxy identity." .
    # calculated: calculated

effortless-ntwf:geographicTypeDFraction a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "GeographicTypeDFraction" ;
    rdfs:comment "Type-D fraction for geographic-composition identity." .
    # calculated: calculated

effortless-ntwf:severityDriftDirection a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "SeverityDriftDirection" ;
    rdfs:comment "loop-92: ConfounderDistortionTimeline.DriftDirection for id-disease-severity (severity-case-mix archetype), valid-time Type-D drift. Overridden in 02b-customize-functions.sql: native transpiler mistranslates LOOKUP(constant, Table[NonPKColumn], Table[TargetColumn]) into a lookup keyed on the table's PK instead of the specified match column." .
    # calculated: calculated

effortless-ntwf:geographicDriftDirection a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "GeographicDriftDirection" ;
    rdfs:comment "loop-92: ConfounderDistortionTimeline.DriftDirection for id-geographic-composition (geography archetype), valid-time Type-D drift. Overridden in 02b-customize-functions.sql: native transpiler mistranslates LOOKUP(constant, Table[NonPKColumn], Table[TargetColumn]) into a lookup keyed on the table's PK instead of the specified match column." .
    # calculated: calculated

effortless-ntwf:situationalDriftDirection a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "SituationalDriftDirection" ;
    rdfs:comment "loop-92: ConfounderDistortionTimeline.DriftDirection for id-situational-context (sports-situation archetype), valid-time Type-D drift. Overridden in 02b-customize-functions.sql: native transpiler mistranslates LOOKUP(constant, Table[NonPKColumn], Table[TargetColumn]) into a lookup keyed on the table's PK instead of the specified match column." .
    # calculated: calculated

effortless-ntwf:ageDriftDirection a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "AgeDriftDirection" ;
    rdfs:comment "loop-92: ConfounderDistortionTimeline.DriftDirection for id-age-composition (demographic-composition archetype), valid-time Type-D drift. Overridden in 02b-customize-functions.sql: native transpiler mistranslates LOOKUP(constant, Table[NonPKColumn], Table[TargetColumn]) into a lookup keyed on the table's PK instead of the specified match column." .
    # calculated: calculated

effortless-ntwf:institutionalDriftDirection a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "InstitutionalDriftDirection" ;
    rdfs:comment "loop-92: ConfounderDistortionTimeline.DriftDirection for id-institutional-unit (organizational-unit archetype), valid-time Type-D drift. Overridden in 02b-customize-functions.sql: native transpiler mistranslates LOOKUP(constant, Table[NonPKColumn], Table[TargetColumn]) into a lookup keyed on the table's PK instead of the specified match column." .
    # calculated: calculated

effortless-ntwf:socioeconomicDriftDirection a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "SocioeconomicDriftDirection" ;
    rdfs:comment "loop-92: ConfounderDistortionTimeline.DriftDirection for id-socioeconomic-status (ses-income archetype), valid-time Type-D drift. Overridden in 02b-customize-functions.sql: native transpiler mistranslates LOOKUP(constant, Table[NonPKColumn], Table[TargetColumn]) into a lookup keyed on the table's PK instead of the specified match column." .
    # calculated: calculated

effortless-ntwf:domainFlipGapSurvivesGeometryControl a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:boolean ;
    rdfs:label "DomainFlipGapSurvivesGeometryControl" ;
    rdfs:comment "TRUE when the economics-vs-epidemiology sign-flip-rate gap for H-domain-flip-geometry-controlled survives after restricting to high-imbalance studies (loop-90 native promotion of the former 02b hand function)." .
    # calculated: calculated

effortless-ntwf:corpusPatternSupersededFailCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "CorpusPatternSupersededFailCount" ;
    rdfs:comment "Count of DiscoveryFindings rows for the 6 superseded-pattern hypotheses (H-econ-zero, H-domain-dist, H-catalog-flip-prediction, H-domain-flip-geometry-controlled, H-econ-encoding-selection, H-domain-profiles-stable) where IsConfirmed=FALSE. Six separate COUNTIFS summed with '+' because this rulebook's formula vocabulary has no IN-list/pipe-delimited OR criterion for COUNTIFS (loop-90 native promotion of the former 02b hand function)." .
    # calculated: calculated

effortless-ntwf:expansionWave3DiscoveryNote a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ModelSummary ;
    rdfs:range xsd:string ;
    rdfs:label "ExpansionWave3DiscoveryNote" ;
    rdfs:comment "Expansion-wave-3 supersession-audit synthesis rollup (loop-78, promoted to native loop-90)." .
    # calculated: calculated

# === Class: PhaseDiagramSummary ===
effortless-ntwf:PhaseDiagramSummary a owl:Class ;
    rdfs:label "PhaseDiagramSummary" ;
    rdfs:comment "Table: PhaseDiagramSummary — one row summarising the SyntheticPhase grid. Witnesses that all five DistortionType cells (A, B, C+, C-, D) are nonempty in parameter space." .

effortless-ntwf:phaseDiagramId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:PhaseDiagramSummary ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseDiagramId" ;
    rdfs:comment "Unique identifier for this phase diagram instance." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:phasePointCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:PhaseDiagramSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "PhasePointCount" ;
    rdfs:comment "Total grid points in SyntheticPhase (sum of type counts). Refreshed from vw_synthetic_phase by 05b-customize-data.sql." .
    # calculated: calculated

effortless-ntwf:phaseTypeACount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:PhaseDiagramSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "PhaseTypeACount" ;
    rdfs:comment "Grid points classified as type A. Populated from vw_synthetic_phase by 05b-customize-data.sql." .

effortless-ntwf:phaseTypeBCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:PhaseDiagramSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "PhaseTypeBCount" ;
    rdfs:comment "Grid points classified as type B. Populated from vw_synthetic_phase by 05b-customize-data.sql." .

effortless-ntwf:phaseTypeCPlusCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:PhaseDiagramSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "PhaseTypeCPlusCount" ;
    rdfs:comment "Grid points classified as type C+. Populated from vw_synthetic_phase by 05b-customize-data.sql." .

effortless-ntwf:phaseTypeCMinusCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:PhaseDiagramSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "PhaseTypeCMinusCount" ;
    rdfs:comment "Grid points classified as type C-. Populated from vw_synthetic_phase by 05b-customize-data.sql." .

effortless-ntwf:phaseTypeDCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:PhaseDiagramSummary ;
    rdfs:range xsd:integer ;
    rdfs:label "PhaseTypeDCount" ;
    rdfs:comment "Grid points classified as type D. Populated from vw_synthetic_phase by 05b-customize-data.sql." .

effortless-ntwf:allFiveTypesPresent a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:PhaseDiagramSummary ;
    rdfs:range xsd:boolean ;
    rdfs:label "AllFiveTypesPresent" ;
    rdfs:comment "TRUE when every DistortionType cell has at least one grid point." .
    # calculated: calculated

effortless-ntwf:phaseTaxonomyWitness a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:PhaseDiagramSummary ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseTaxonomyWitness" ;
    rdfs:comment "Human-readable witness string of type counts across the grid." .
    # calculated: calculated

effortless-ntwf:phaseWitnessNote a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:PhaseDiagramSummary ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseWitnessNote" ;
    rdfs:comment "PASS/FAIL summary for taxonomy completeness." .
    # calculated: calculated

# === Class: ResearchTraditions ===
effortless-ntwf:ResearchTraditions a owl:Class ;
    rdfs:label "ResearchTraditions" ;
    rdfs:comment "Table: ResearchTraditions — the six scholarly lineages that have engaged with Simpson's Paradox. Each row is a named intellectual tradition, not a specific paper or result." .

effortless-ntwf:traditionId a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionId" ;
    rdfs:comment "Slug identifier, e.g. tradition-historical." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Short display name for the tradition." .

effortless-ntwf:era a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ResearchTraditions ;
    rdfs:range xsd:string ;
    rdfs:label "Era" ;
    rdfs:comment "Approximate active period, e.g. \"1899–1972\"." .

effortless-ntwf:coreConcern a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ResearchTraditions ;
    rdfs:range xsd:string ;
    rdfs:label "CoreConcern" ;
    rdfs:comment "The central question this tradition asks about the paradox." .

effortless-ntwf:primaryVenue a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ResearchTraditions ;
    rdfs:range xsd:string ;
    rdfs:label "PrimaryVenue" ;
    rdfs:comment "Where this tradition publishes (journal, conference, book series)." .

effortless-ntwf:keyClaim a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ResearchTraditions ;
    rdfs:range xsd:string ;
    rdfs:label "KeyClaim" ;
    rdfs:comment "The tradition's signature thesis or resolution strategy." .

effortless-ntwf:openQuestion a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ResearchTraditions ;
    rdfs:range xsd:string ;
    rdfs:label "OpenQuestion" ;
    rdfs:comment "What this tradition leaves unresolved or contested." .

effortless-ntwf:verifiedByDeepResearch a owl:DatatypeProperty ;
    rdfs:range xsd:boolean ;
    rdfs:label "VerifiedByDeepResearch" ;
    rdfs:comment "True if the 2026-06-28 adversarial deep-research pass confirmed the tradition at high confidence." .

effortless-ntwf:illustratedByStudy a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ResearchTraditions ;
    rdfs:range xsd:string ;
    rdfs:label "IllustratedByStudy" ;
    rdfs:comment "FK → Studies.StudyId. The study in this model that most vividly illustrates this tradition's characteristic concern or resolution strategy." .

effortless-ntwf:supportingConclusion a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ResearchTraditions ;
    rdfs:range xsd:string ;
    rdfs:label "SupportingConclusion" ;
    rdfs:comment "FK → Conclusions.ConclusionId. The conclusion in this model that most directly validates or advances this tradition's central claim about Simpson's Paradox." .

effortless-ntwf:illustratedByStudyTitle a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ResearchTraditions ;
    rdfs:range xsd:string ;
    rdfs:label "IllustratedByStudyTitle" ;
    rdfs:comment "Lookup: Studies.Title via IllustratedByStudy." .
    # calculated: lookup

effortless-ntwf:illustratedByStudySource a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ResearchTraditions ;
    rdfs:range xsd:string ;
    rdfs:label "IllustratedByStudySource" ;
    rdfs:comment "Lookup: Studies.Source via IllustratedByStudy — the citation for the study that best illustrates this tradition." .
    # calculated: lookup

effortless-ntwf:illustratedByStudyDistortionType a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ResearchTraditions ;
    rdfs:range xsd:string ;
    rdfs:label "IllustratedByStudyDistortionType" ;
    rdfs:comment "Lookup: TreatmentRankings.DistortionType for the illustrated study." .
    # calculated: lookup

effortless-ntwf:supportingConclusionTitle a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ResearchTraditions ;
    rdfs:range xsd:string ;
    rdfs:label "SupportingConclusionTitle" ;
    rdfs:comment "Lookup: Conclusions.Title via SupportingConclusion." .
    # calculated: lookup

effortless-ntwf:supportingConclusionCategory a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ResearchTraditions ;
    rdfs:range xsd:string ;
    rdfs:label "SupportingConclusionCategory" ;
    rdfs:comment "Lookup: Conclusions.Category via SupportingConclusion." .
    # calculated: lookup

effortless-ntwf:researcherCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:ResearchTraditions ;
    rdfs:range xsd:integer ;
    rdfs:label "ResearcherCount" ;
    rdfs:comment "Count of researchers in this tradition." .
    # calculated: aggregation

effortless-ntwf:studyCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "StudyCount" ;
    rdfs:comment "Count of studies in this model whose TraditionId matches this tradition." .
    # calculated: aggregation

effortless-ntwf:loopCount a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "LoopCount" ;
    rdfs:comment "Count of Effortless Loops that drew primarily on this tradition's conceptual vocabulary." .
    # calculated: aggregation

# === Class: Researchers ===
effortless-ntwf:Researchers a owl:Class ;
    rdfs:label "Researchers" ;
    rdfs:comment "Table: Researchers — individuals who have made named, verified contributions to understanding Simpson's Paradox. Linked to ResearchTraditions by TraditionId." .

effortless-ntwf:researcherId a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "ResearcherId" ;
    rdfs:comment "Slug, e.g. researcher-pearson." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Full name as commonly cited." .

effortless-ntwf:traditionId a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionId" ;
    rdfs:comment "FK → ResearchTraditions.TraditionId" .

effortless-ntwf:activePeriod a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Researchers ;
    rdfs:range xsd:string ;
    rdfs:label "ActivePeriod" ;
    rdfs:comment "Approximate years of relevant work, e.g. \"1899\"." .

effortless-ntwf:affiliation a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Researchers ;
    rdfs:range xsd:string ;
    rdfs:label "Affiliation" ;
    rdfs:comment "Primary institutional affiliation for the relevant work." .

effortless-ntwf:keyContribution a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Researchers ;
    rdfs:range xsd:string ;
    rdfs:label "KeyContribution" ;
    rdfs:comment "One-sentence description of their specific contribution to the paradox literature." .

effortless-ntwf:signatureWork a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Researchers ;
    rdfs:range xsd:string ;
    rdfs:label "SignatureWork" ;
    rdfs:comment "The canonical paper or book most associated with their contribution." .

effortless-ntwf:relationToHannanPearl a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Researchers ;
    rdfs:range xsd:string ;
    rdfs:label "RelationToHannanPearl" ;
    rdfs:comment "How this researcher's line of work relates to or differs from Hannan (aggregation bias) and Pearl (DAG/do-calculus)." .

effortless-ntwf:verifiedByDeepResearch a owl:DatatypeProperty ;
    rdfs:range xsd:boolean ;
    rdfs:label "VerifiedByDeepResearch" ;
    rdfs:comment "True if the 2026-06-28 adversarial deep-research pass confirmed this person at high confidence." .

effortless-ntwf:confidenceLevel a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Researchers ;
    rdfs:range xsd:string ;
    rdfs:label "ConfidenceLevel" ;
    rdfs:comment "high | medium | unverified — matches deep-research vote outcome." .

effortless-ntwf:canonicalStudyId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Researchers ;
    rdfs:range xsd:string ;
    rdfs:label "CanonicalStudyId" ;
    rdfs:comment "FK → Studies.StudyId. The study in this model that best illustrates this researcher's specific contribution to the Simpson's Paradox literature." .

effortless-ntwf:illustratesConclusion a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Researchers ;
    rdfs:range xsd:string ;
    rdfs:label "IllustratesConclusion" ;
    rdfs:comment "FK → Conclusions.ConclusionId. The conclusion in this model that this researcher's work directly supports, challenges, or frames." .

effortless-ntwf:traditionName a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionName" ;
    rdfs:comment "Lookup: ResearchTraditions.Name via TraditionId." .
    # calculated: lookup

effortless-ntwf:traditionEra a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Researchers ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionEra" ;
    rdfs:comment "Lookup: ResearchTraditions.Era via TraditionId." .
    # calculated: lookup

effortless-ntwf:canonicalStudyTitle a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Researchers ;
    rdfs:range xsd:string ;
    rdfs:label "CanonicalStudyTitle" ;
    rdfs:comment "Lookup: Studies.Title via CanonicalStudyId." .
    # calculated: lookup

effortless-ntwf:canonicalStudyDistortionType a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Researchers ;
    rdfs:range xsd:string ;
    rdfs:label "CanonicalStudyDistortionType" ;
    rdfs:comment "Lookup: TreatmentRankings.DistortionType for the canonical study — the geometric type of paradox this researcher's work exemplifies." .
    # calculated: lookup

effortless-ntwf:canonicalStudyPolicyImplication a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Researchers ;
    rdfs:range xsd:string ;
    rdfs:label "CanonicalStudyPolicyImplication" ;
    rdfs:comment "Lookup: TreatmentRankings.CorrectedPolicyImplication for the canonical study." .
    # calculated: lookup

effortless-ntwf:illustratesConclusionTitle a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Researchers ;
    rdfs:range xsd:string ;
    rdfs:label "IllustratesConclusionTitle" ;
    rdfs:comment "Lookup: Conclusions.Title via IllustratesConclusion." .
    # calculated: lookup

effortless-ntwf:traditionKeyClaimSummary a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionKeyClaimSummary" ;
    rdfs:comment "Lookup: ResearchTraditions.KeyClaim via TraditionId — the tradition's signature thesis this researcher contributed to." .
    # calculated: lookup

# === Class: Strata ===
effortless-ntwf:Strata a owl:Class ;
    rdfs:label "Strata" ;
    rdfs:comment "Table: Strata — the subdivisions of patients within a study by the value of the confounding variable (e.g. stone size). Each stratum belongs to one study." .

effortless-ntwf:stratumId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Strata ;
    rdfs:range xsd:string ;
    rdfs:label "StratumId" ;
    rdfs:comment "Unique slug (e.g. kidney-1986-small, kidney-1986-large)." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:study a owl:ObjectProperty ;
    rdfs:range effortless-ntwf:Studies ;
    rdfs:label "Study" ;
    rdfs:comment "FK to the parent Study." .

effortless-ntwf:stratumLabel a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "StratumLabel" ;
    rdfs:comment "Short label used in CaseCells (e.g. small, large, dept-A)." .

effortless-ntwf:description a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Description" ;
    rdfs:comment "Plain-language description of what defines this stratum." .

effortless-ntwf:totalCases a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "TotalCases" ;
    rdfs:comment "Total cases in this stratum across all treatments." .
    # calculated: aggregation

# === Class: StratumSummaries ===
effortless-ntwf:StratumSummaries a owl:Class ;
    rdfs:label "StratumSummaries" ;
    rdfs:comment "Table: StratumSummaries — one row per (study, stratum, treatment). Computes the success rate WITHIN each stratum for each treatment. This is the stratified view that contradicts the pooled conclusion when a Simpson's Paradox reversal is present. Each row belongs to both a Stratum and a Treatment in the same Study." .

effortless-ntwf:stratumSummaryId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:string ;
    rdfs:label "StratumSummaryId" ;
    rdfs:comment "Unique slug (e.g. kidney-1986-small-A)." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:study a owl:ObjectProperty ;
    rdfs:range effortless-ntwf:Studies ;
    rdfs:label "Study" ;
    rdfs:comment "FK to the parent Study." .

effortless-ntwf:stratumLabel a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "StratumLabel" ;
    rdfs:comment "Which stratum (matches Strata.StratumLabel)." .

effortless-ntwf:treatmentLabel a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TreatmentLabel" ;
    rdfs:comment "Which treatment (matches Treatments.TreatmentLabel)." .

effortless-ntwf:stratumSuccesses a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:integer ;
    rdfs:label "StratumSuccesses" ;
    rdfs:comment "Total successes in this (stratum, treatment) cell for this study." .
    # calculated: aggregation

effortless-ntwf:stratumCases a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:integer ;
    rdfs:label "StratumCases" ;
    rdfs:comment "Total cases in this (stratum, treatment) cell for this study." .
    # calculated: aggregation

effortless-ntwf:stratumSuccessRate a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:string ;
    rdfs:label "StratumSuccessRate" ;
    rdfs:comment "Success rate within this stratum for this treatment: StratumSuccesses / StratumCases. The true per-stratum picture." .
    # calculated: calculated

effortless-ntwf:stratumSuccessesA a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:integer ;
    rdfs:label "StratumSuccessesA" ;
    rdfs:comment "Successes for treatment A in this (study, stratum) — used to determine the per-stratum winner." .
    # calculated: aggregation

effortless-ntwf:stratumCasesA a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:integer ;
    rdfs:label "StratumCasesA" ;
    rdfs:comment "Cases for treatment A in this (study, stratum)." .
    # calculated: aggregation

effortless-ntwf:stratumRateA a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:string ;
    rdfs:label "StratumRateA" ;
    rdfs:comment "Success rate for treatment A in this stratum." .
    # calculated: calculated

effortless-ntwf:stratumSuccessesB a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:integer ;
    rdfs:label "StratumSuccessesB" ;
    rdfs:comment "Successes for treatment B in this (study, stratum)." .
    # calculated: aggregation

effortless-ntwf:stratumCasesB a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:integer ;
    rdfs:label "StratumCasesB" ;
    rdfs:comment "Cases for treatment B in this (study, stratum)." .
    # calculated: aggregation

effortless-ntwf:stratumRateB a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:string ;
    rdfs:label "StratumRateB" ;
    rdfs:comment "Success rate for treatment B in this stratum." .
    # calculated: calculated

effortless-ntwf:stratumWinner a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:string ;
    rdfs:label "StratumWinner" ;
    rdfs:comment "Which treatment wins in this stratum: 'A' if StratumRateA > StratumRateB, else 'B'. Used by TreatmentRankings to detect paradox." .
    # calculated: calculated

effortless-ntwf:studyTotalCases a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:integer ;
    rdfs:label "StudyTotalCases" ;
    rdfs:comment "Total cases across ALL strata and ALL treatments in this study. The denominator for StratumFraction." .
    # calculated: aggregation

effortless-ntwf:stratumTotalCases a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:integer ;
    rdfs:label "StratumTotalCases" ;
    rdfs:comment "Total cases in this stratum across both treatments (A + B combined). Numerator for StratumFraction." .
    # calculated: aggregation

effortless-ntwf:stratumFraction a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:string ;
    rdfs:label "StratumFraction" ;
    rdfs:comment "Fraction of the study's total cases that fall in this stratum: StratumTotalCases / StudyTotalCases. The weight the confounder assigns to each stratum. A large StratumFraction on a low-success stratum pulls the pooled rate down; when that stratum is also over-allocated to one treatment, it creates the reversal." .
    # calculated: calculated

effortless-ntwf:weightedStratumRate a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:string ;
    rdfs:label "WeightedStratumRate" ;
    rdfs:comment "StratumFraction × StratumSuccessRate. The contribution of this (stratum, treatment) row to the study-level pooled rate. Summing WeightedStratumRate across all strata for a given treatment reconstructs that treatment's pooled rate from stratum weights — witnessing the mechanism equation." .
    # calculated: calculated

effortless-ntwf:treatmentACasesHere a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:integer ;
    rdfs:label "TreatmentACasesHere" ;
    rdfs:comment "Cases for treatment A in this stratum — used to compute TreatmentA's allocation fraction in this stratum." .
    # calculated: aggregation

effortless-ntwf:treatmentBCasesHere a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:integer ;
    rdfs:label "TreatmentBCasesHere" ;
    rdfs:comment "Cases for treatment B in this stratum — used to compute TreatmentB's allocation fraction in this stratum." .
    # calculated: aggregation

effortless-ntwf:treatmentATotalCases a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:integer ;
    rdfs:label "TreatmentATotalCases" ;
    rdfs:comment "Total cases for treatment A across all strata in this study — denominator for AllocationFractionA." .
    # calculated: aggregation

effortless-ntwf:treatmentBTotalCases a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:integer ;
    rdfs:label "TreatmentBTotalCases" ;
    rdfs:comment "Total cases for treatment B across all strata in this study — denominator for AllocationFractionB." .
    # calculated: aggregation

effortless-ntwf:allocationFractionA a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:string ;
    rdfs:label "AllocationFractionA" ;
    rdfs:comment "Fraction of treatment A's total cases that land in this stratum: TreatmentACasesHere / TreatmentATotalCases. Equivalent to TreatmentExposureFraction for A." .
    # calculated: calculated

effortless-ntwf:allocationFractionB a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:string ;
    rdfs:label "AllocationFractionB" ;
    rdfs:comment "Fraction of treatment B's total cases that land in this stratum: TreatmentBCasesHere / TreatmentBTotalCases." .
    # calculated: calculated

effortless-ntwf:allocationBias a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:string ;
    rdfs:label "AllocationBias" ;
    rdfs:comment "AllocationFractionA minus AllocationFractionB: how much more of treatment A (relative to B) is concentrated in this stratum. Positive = A is over-allocated here; negative = B is over-allocated here. When AllocationBias is large and negative in a low-success stratum, treatment A is under-represented in the hard cases — making A look worse in the pooled view than it is in any stratum. This is the confounder's fingerprint as a signed number." .
    # calculated: calculated

effortless-ntwf:stratumGap a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:string ;
    rdfs:label "StratumGap" ;
    rdfs:comment "Signed rate difference within this stratum: StratumRateA minus StratumRateB. Positive = A wins here; negative = B wins here; zero = tied. When every stratum has StratumGap > 0 but the pooled gap is negative, the contradiction IS Simpson's Paradox made arithmetically visible in one column. Multiply StratumGap × StratumFraction and sum across strata to get the equal-weight pooled gap (the signal allocation would produce without confounding)." .
    # calculated: calculated

effortless-ntwf:weightedStratumGap a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:string ;
    rdfs:label "WeightedStratumGap" ;
    rdfs:comment "StratumGap × StratumFraction: the contribution of this stratum's treatment-rate difference to the equal-weight pooled gap. Summing WeightedStratumGap across all TreatmentA rows for a study gives WeightedStratumGapSum — the pooled gap the study would show under balanced allocation. This is the per-stratum building block of the sign-flip test." .
    # calculated: calculated

effortless-ntwf:absAllocationBias a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:string ;
    rdfs:label "AbsAllocationBias" ;
    rdfs:comment "ABS(AllocationBias). MAXIFS-friendly unsigned magnitude so TreatmentRankings.MaxStratumImbalance can compute live via MAXIFS instead of a hand-written cache." .
    # calculated: calculated

effortless-ntwf:absStratumGap a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumSummaries ;
    rdfs:range xsd:string ;
    rdfs:label "AbsStratumGap" ;
    rdfs:comment "ABS(StratumGap). MAXIFS-friendly unsigned magnitude so TreatmentRankings.MaxStratumGap can compute live via MAXIFS instead of a hand-written cache." .
    # calculated: calculated

# === Class: StratumVariableIdentityMaps ===
effortless-ntwf:StratumVariableIdentityMaps a owl:Class ;
    rdfs:label "StratumVariableIdentityMaps" ;
    rdfs:comment "Table: StratumVariableIdentityMaps — many-to-one map from per-study StratumVariables to canonical ConfounderIdentities (loop-80)." .

effortless-ntwf:mapId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumVariableIdentityMaps ;
    rdfs:range xsd:string ;
    rdfs:label "MapId" ;
    rdfs:comment "Unique slug." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:stratumVariable a owl:ObjectProperty ;
    rdfs:domain effortless-ntwf:StratumVariableIdentityMaps ;
    rdfs:range effortless-ntwf:StratumVariables ;
    rdfs:label "StratumVariable" ;
    rdfs:comment "FK → StratumVariables." .

effortless-ntwf:confounderIdentity a owl:ObjectProperty ;
    rdfs:range effortless-ntwf:ConfounderIdentities ;
    rdfs:label "ConfounderIdentity" ;
    rdfs:comment "FK → ConfounderIdentities." .

effortless-ntwf:variableNameNormalized a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumVariableIdentityMaps ;
    rdfs:range xsd:string ;
    rdfs:label "VariableNameNormalized" ;
    rdfs:comment "Lowercase normalized variable token for dedup audit." .

# === Class: StratumVariables ===
effortless-ntwf:StratumVariables a owl:Class ;
    rdfs:label "StratumVariables" ;
    rdfs:comment "Table: StratumVariables — one row per study, naming the variable that defines the strata and describing its causal role. A confounder affects both treatment assignment AND outcome; a mediator is downstream of treatment; a contested variable is one where the causal role is disputed. IsConfounder is derived from these two boolean flags." .

effortless-ntwf:stratumVariableId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumVariables ;
    rdfs:range xsd:string ;
    rdfs:label "StratumVariableId" ;
    rdfs:comment "Unique slug (e.g. kidney-1986-stone-size)." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:study a owl:ObjectProperty ;
    rdfs:range effortless-ntwf:Studies ;
    rdfs:label "Study" ;
    rdfs:comment "FK to the parent Study." .

effortless-ntwf:variableName a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumVariables ;
    rdfs:range xsd:string ;
    rdfs:label "VariableName" ;
    rdfs:comment "The name of the stratifying variable (e.g. stone_size, department, age_group)." .

effortless-ntwf:causalRole a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "CausalRole" ;
    rdfs:comment "The variable's causal role: 'confounder' (affects both assignment and outcome), 'mediator' (downstream of treatment), 'contested' (causal role disputed), 'unknown'." .

effortless-ntwf:affectsTreatmentAssignment a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumVariables ;
    rdfs:range xsd:boolean ;
    rdfs:label "AffectsTreatmentAssignment" ;
    rdfs:comment "TRUE if this variable causally influences which treatment a patient receives. Required for confounding." .

effortless-ntwf:affectsOutcome a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumVariables ;
    rdfs:range xsd:boolean ;
    rdfs:label "AffectsOutcome" ;
    rdfs:comment "TRUE if this variable causally influences the outcome independently of treatment. Required for confounding." .

effortless-ntwf:isConfounder a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumVariables ;
    rdfs:range xsd:boolean ;
    rdfs:label "IsConfounder" ;
    rdfs:comment "TRUE when AffectsTreatmentAssignment AND AffectsOutcome AND CausalRole = 'confounder'. A confounder is the classic driver of Simpson's Paradox." .
    # calculated: calculated

effortless-ntwf:mechanismNote a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumVariables ;
    rdfs:range xsd:string ;
    rdfs:label "MechanismNote" ;
    rdfs:comment "Plain-language description of how this variable creates the confounding: why it affects both assignment and outcome." .

effortless-ntwf:conditioningRisk a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumVariables ;
    rdfs:range xsd:string ;
    rdfs:label "ConditioningRisk" ;
    rdfs:comment "The causal hazard posed by conditioning on this variable. Values: confounder (safe to condition — removes bias), mediator (conditioning blocks the causal path of interest — harmful), collider (conditioning opens a spurious backdoor — harmful), proxy (conditioning on a noisy proxy of the true confounder — risky), none (no conditioning risk identified). NULL means unclassified." .

effortless-ntwf:confounderIdentity a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "ConfounderIdentity" ;
    rdfs:comment "Canonical cross-study identity via StratumVariableIdentityMaps (loop-80)." .
    # calculated: lookup

effortless-ntwf:annotationCommitHash a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumVariables ;
    rdfs:range xsd:string ;
    rdfs:label "AnnotationCommitHash" ;
    rdfs:comment "Full git SHA of the commit that first introduced this row's CausalRole/MechanismNote — derived via `git log -S` pickaxe search on this row's StratumVariableId (loop-98 audit-trail backfill). Real repo history, not a hand-entered label." .

effortless-ntwf:annotationCommitDate a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumVariables ;
    rdfs:range xsd:string ;
    rdfs:label "AnnotationCommitDate" ;
    rdfs:comment "ISO date (YYYY-MM-DD) of AnnotationCommitHash." .

effortless-ntwf:annotationCommitSubject a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumVariables ;
    rdfs:range xsd:string ;
    rdfs:label "AnnotationCommitSubject" ;
    rdfs:comment "Git commit subject line of AnnotationCommitHash — the loop/change that landed this row's causal-role judgment call." .

effortless-ntwf:causalRoleEverRevised a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StratumVariables ;
    rdfs:range xsd:boolean ;
    rdfs:label "CausalRoleEverRevised" ;
    rdfs:comment "TRUE if this row's CausalRole value differs across any two points in its git history (a real reclassification); FALSE if the value has been identical since AnnotationCommitHash. Computed once at loop-97 backfill via full-history diff, not re-derived live — re-run the backfill script after any future CausalRole edit to keep this accurate." .

# === Class: Studies ===
effortless-ntwf:Studies a owl:Class ;
    rdfs:label "Studies" ;
    rdfs:comment "Table: Studies — a clinical or empirical study comparing two or more treatments for a condition, with patient cases stratified by a confounding variable. The top-level entity in the domain." .

effortless-ntwf:studyId a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "StudyId" ;
    rdfs:comment "Unique slug for this study (e.g. kidney-1986, berkeley-1973)." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label, mirrors StudyId." .
    # calculated: calculated

effortless-ntwf:title a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Title" ;
    rdfs:comment "Human-readable title of the study." .

effortless-ntwf:source a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Studies ;
    rdfs:range xsd:string ;
    rdfs:label "Source" ;
    rdfs:comment "Citation or description of the original data source." .

effortless-ntwf:sourceUrl a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SourceUrl" ;
    rdfs:comment "URL to the original source." .

effortless-ntwf:publicationYear a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "PublicationYear" ;
    rdfs:comment "Year the study was published. Used for temporal stratification and pre-registration dating." .

effortless-ntwf:domain a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Domain" ;
    rdfs:comment "Research domain: medicine | epidemiology | social-science | education | legal | sports | economics | synthetic. Enables cross-domain SignalPurity stratification." .

effortless-ntwf:isSynthetic a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Studies ;
    rdfs:range xsd:boolean ;
    rdfs:label "IsSynthetic" ;
    rdfs:comment "TRUE for constructed/counterfactual studies; FALSE for real published studies. Filters the corpus for empirical claims." .

effortless-ntwf:totalCases a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "TotalCases" ;
    rdfs:comment "Total number of cases (trials) across all CaseCells in this study." .
    # calculated: aggregation

effortless-ntwf:cellCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Studies ;
    rdfs:range xsd:integer ;
    rdfs:label "CellCount" ;
    rdfs:comment "Number of CaseCell rows for this study." .
    # calculated: aggregation

effortless-ntwf:traditionId a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionId" ;
    rdfs:comment "FK → ResearchTraditions.TraditionId. The scholarly tradition whose framing best characterizes this study's paradox type. Synthetic studies inherit the tradition of their conceptual origin." .

effortless-ntwf:primaryResearcherId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Studies ;
    rdfs:range xsd:string ;
    rdfs:label "PrimaryResearcherId" ;
    rdfs:comment "FK → Researchers.ResearcherId. The researcher most associated with first surfacing or canonically citing this study in the Simpson's Paradox literature." .

effortless-ntwf:traditionName a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionName" ;
    rdfs:comment "Lookup: ResearchTraditions.Name via TraditionId." .
    # calculated: lookup

effortless-ntwf:traditionCoreConern a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionCoreConern" ;
    rdfs:comment "Lookup: ResearchTraditions.CoreConcern via TraditionId." .
    # calculated: lookup

effortless-ntwf:primaryResearcherName a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Studies ;
    rdfs:range xsd:string ;
    rdfs:label "PrimaryResearcherName" ;
    rdfs:comment "Lookup: Researchers.Name via PrimaryResearcherId." .
    # calculated: lookup

effortless-ntwf:primaryResearcherAffiliation a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Studies ;
    rdfs:range xsd:string ;
    rdfs:label "PrimaryResearcherAffiliation" ;
    rdfs:comment "Lookup: Researchers.Affiliation via PrimaryResearcherId." .
    # calculated: lookup

effortless-ntwf:distortionType a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "DistortionType" ;
    rdfs:comment "Lookup: TreatmentRankings.DistortionType for this study — the geometric classification of its paradox." .
    # calculated: lookup

effortless-ntwf:policyImplication a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "PolicyImplication" ;
    rdfs:comment "Lookup: TreatmentRankings.CorrectedPolicyImplication for this study." .
    # calculated: lookup

effortless-ntwf:allocationDistortion a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "AllocationDistortion" ;
    rdfs:comment "Lookup: TreatmentRankings.AllocationDistortion for this study — the scalar severity of allocation-induced distortion." .
    # calculated: lookup

effortless-ntwf:signalPurity a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SignalPurity" ;
    rdfs:comment "Lookup: TreatmentRankings.SignalPurity for this study — ratio of true signal to total gap width." .
    # calculated: lookup

effortless-ntwf:isSignFlip a owl:DatatypeProperty ;
    rdfs:range xsd:boolean ;
    rdfs:label "IsSignFlip" ;
    rdfs:comment "Lookup: TreatmentRankings.IsSignFlip for this study — true if the paradox satisfies the continuous sign-flip definition." .
    # calculated: lookup

effortless-ntwf:correctedWinner a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "CorrectedWinner" ;
    rdfs:comment "Lookup: TreatmentRankings.CorrectedWinner for this study — which treatment wins after allocation correction." .
    # calculated: lookup

effortless-ntwf:confoundingVariable a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Studies ;
    rdfs:range xsd:string ;
    rdfs:label "ConfoundingVariable" ;
    rdfs:comment "Lookup: StratumVariables.VariableName for this study — the named stratifying variable." .
    # calculated: lookup

effortless-ntwf:causalRole a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "CausalRole" ;
    rdfs:comment "Lookup: StratumVariables.CausalRole for this study — confounder, mediator, collider, proxy, or contested." .
    # calculated: lookup

effortless-ntwf:ingestionCellParity a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Studies ;
    rdfs:range xsd:boolean ;
    rdfs:label "IngestionCellParity" ;
    rdfs:comment "TRUE when CellCount = 2 x StratumCount." .
    # calculated: calculated

effortless-ntwf:ingestionCompliance a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Studies ;
    rdfs:range xsd:string ;
    rdfs:label "IngestionCompliance" ;
    rdfs:comment "all | partial | exempt-synthetic." .
    # calculated: calculated

effortless-ntwf:isControlStudy a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Studies ;
    rdfs:range xsd:boolean ;
    rdfs:label "IsControlStudy" ;
    rdfs:comment "TRUE if this study is a control (no Simpson's paradox expected/found); FALSE for the main Simpson's Paradox corpus." .

effortless-ntwf:controlDomain a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Studies ;
    rdfs:range xsd:string ;
    rdfs:label "ControlDomain" ;
    rdfs:comment "Domain slug for control studies (e.g. medicine-rct, education-higher). NULL for non-control studies." .

effortless-ntwf:validTimeStart a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Studies ;
    rdfs:range xsd:integer ;
    rdfs:label "ValidTimeStart" ;
    rdfs:comment "First year of the real-world cohort period this study's data covers (valid-time, not publication year). Defaults to PublicationYear when the true cohort window is not independently known." .

effortless-ntwf:validTimeEnd a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Studies ;
    rdfs:range xsd:integer ;
    rdfs:label "ValidTimeEnd" ;
    rdfs:comment "Last year of the real-world cohort period this study's data covers (valid-time, not publication year). Defaults to PublicationYear when the true cohort window is not independently known." .

effortless-ntwf:validTimeMidpoint a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Studies ;
    rdfs:range xsd:string ;
    rdfs:label "ValidTimeMidpoint" ;
    rdfs:comment "Midpoint year of the valid-time cohort window, used to bucket studies by when their underlying data actually occurred rather than when the paper was published." .
    # calculated: calculated

effortless-ntwf:validTimeDecade a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "ValidTimeDecade" ;
    rdfs:comment "ValidTimeMidpoint floored to the decade (e.g. 1973 -> 1970). The bucket key ConfounderDistortionTimeline aggregates over." .
    # calculated: calculated

# === Class: StudyImportTemplate ===
effortless-ntwf:StudyImportTemplate a owl:Class ;
    rdfs:label "StudyImportTemplate" ;
    rdfs:comment "Table: StudyImportTemplate — mechanical checklist of rulebook rows to create when importing one catalog candidate." .

effortless-ntwf:templateStepId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StudyImportTemplate ;
    rdfs:range xsd:string ;
    rdfs:label "TemplateStepId" ;
    rdfs:comment "Unique slug." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:sortOrder a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "SortOrder" ;
    rdfs:comment "Order to execute during import." .

effortless-ntwf:targetTable a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TargetTable" ;
    rdfs:comment "Rulebook table to write." .

effortless-ntwf:rowDescription a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:StudyImportTemplate ;
    rdfs:range xsd:string ;
    rdfs:label "RowDescription" ;
    rdfs:comment "What rows to create." .

effortless-ntwf:requiredFields a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "RequiredFields" ;
    rdfs:comment "Key columns that must be populated." .

effortless-ntwf:mechanicalCheck a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "MechanicalCheck" ;
    rdfs:comment "How to verify step complete." .

# === Class: SubstrateConformanceFields ===
effortless-ntwf:SubstrateConformanceFields a owl:Class ;
    rdfs:label "SubstrateConformanceFields" ;
    rdfs:comment "Table: SubstrateConformanceFields — cross-substrate OWL-SHACL vs Postgres compare manifest for owl/reason.py. Rows with AssertFromPostgres=TRUE are asserted as leaf facts before SHACL fixpoint (OWL transpiler lacks CONSTRUCT for nested IF chains)." .

effortless-ntwf:fieldId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SubstrateConformanceFields ;
    rdfs:range xsd:string ;
    rdfs:label "FieldId" ;
    rdfs:comment "Unique slug." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:sourceTable a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SourceTable" ;
    rdfs:comment "TreatmentRankings | StratumSummaries | StratumVariables" .

effortless-ntwf:fieldName a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "FieldName" ;
    rdfs:comment "PascalCase rulebook field name." .

effortless-ntwf:pgColumn a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SubstrateConformanceFields ;
    rdfs:range xsd:string ;
    rdfs:label "PgColumn" ;
    rdfs:comment "snake_case vw_* column." .

effortless-ntwf:owlLocalName a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SubstrateConformanceFields ;
    rdfs:range xsd:string ;
    rdfs:label "OwlLocalName" ;
    rdfs:comment "camelCase OWL property local name." .

effortless-ntwf:dataType a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "DataType" ;
    rdfs:comment "float | int | bool | str" .

effortless-ntwf:assertFromPostgres a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SubstrateConformanceFields ;
    rdfs:range xsd:boolean ;
    rdfs:label "AssertFromPostgres" ;
    rdfs:comment "TRUE when OWL cannot derive this field yet and Postgres value is asserted before SHACL fixpoint." .

effortless-ntwf:inCompareSet a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SubstrateConformanceFields ;
    rdfs:range xsd:boolean ;
    rdfs:label "InCompareSet" ;
    rdfs:comment "TRUE when reason.py diffs this field between substrates." .

# === Class: SweepStudyConfig ===
effortless-ntwf:SweepStudyConfig a owl:Class ;
    rdfs:label "SweepStudyConfig" ;
    rdfs:comment "Table: SweepStudyConfig — per-study allocation-sweep parameters derived from CaseCells. Picks the highest-allocation-bias stratum to sweep; holds all other strata fixed." .

effortless-ntwf:configId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SweepStudyConfig ;
    rdfs:range xsd:string ;
    rdfs:label "ConfigId" ;
    rdfs:comment "Unique slug; mirrors StudyId." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:studyId a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "StudyId" ;
    rdfs:comment "FK to Studies.StudyId." .

effortless-ntwf:sweepStratumLabel a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SweepStratumLabel" ;
    rdfs:comment "Stratum whose A/B allocation is swept (highest |AllocationBias| in original data)." .

effortless-ntwf:nSweepStratumTotal a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "NSweepStratumTotal" ;
    rdfs:comment "Total cases in the sweep stratum (fixed across sweep rows)." .

effortless-ntwf:sweepRateA a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SweepRateA" ;
    rdfs:comment "Success rate for treatment A in the sweep stratum." .

effortless-ntwf:sweepRateB a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SweepRateB" ;
    rdfs:comment "Success rate for treatment B in the sweep stratum." .

effortless-ntwf:nFixedA a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "NFixedA" ;
    rdfs:comment "A cases in all non-swept strata (fixed)." .

effortless-ntwf:nFixedB a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "NFixedB" ;
    rdfs:comment "B cases in all non-swept strata (fixed)." .

effortless-ntwf:fixedRateA a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "FixedRateA" ;
    rdfs:comment "Weighted success rate for A in fixed strata." .

effortless-ntwf:fixedRateB a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "FixedRateB" ;
    rdfs:comment "Weighted success rate for B in fixed strata." .

effortless-ntwf:originalAllocFractionA a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SweepStudyConfig ;
    rdfs:range xsd:string ;
    rdfs:label "OriginalAllocFractionA" ;
    rdfs:comment "Observed fraction of sweep-stratum cases assigned to treatment A." .

effortless-ntwf:sweepCorrectedGap a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SweepCorrectedGap" ;
    rdfs:comment "Allocation-free gap: sum_s (n_s/N)*(rate_As-rate_Bs). Invariant under within-stratum reallocation." .

# === Class: SweepStudySummary ===
effortless-ntwf:SweepStudySummary a owl:Class ;
    rdfs:label "SweepStudySummary" .

effortless-ntwf:sweepStudyId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SweepStudySummary ;
    rdfs:range xsd:string ;
    rdfs:label "SweepStudyId" ;
    rdfs:comment "FK to Studies.StudyId. One row per study in the AllocationSweep." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:distortionTypeLabel a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SweepStudySummary ;
    rdfs:range xsd:string ;
    rdfs:label "DistortionTypeLabel" ;
    rdfs:comment "DistortionType for this study (A, B, C, or D) — provides context for what the sweep should show." .
    # calculated: calculated

effortless-ntwf:correctedGapConstant a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SweepStudySummary ;
    rdfs:range xsd:string ;
    rdfs:label "CorrectedGapConstant" ;
    rdfs:comment "Principled materialization (refresh_sweep_study_summary(), 03b/05b customize SQL). The allocation-free corrected gap for this study = MINIFS(AllocationSweep[SweepCorrectedGap], AllocationSweep[StudyId], SweepStudyId). Upstream (AllocationSweep, SweepStudyConfig) is sealed/stable per build; recomputing this per-row via nested calc_* calls was measured to fan out to 22.5s for a 242-study corpus scan with zero memoization — materialized here instead of left as a live calculated field. See MaterializedEntities." .

effortless-ntwf:sweepCorrectedGapMax a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SweepCorrectedGapMax" ;
    rdfs:comment "Principled materialization (refresh_sweep_study_summary()). Maximum SweepCorrectedGap across all allocation fractions for this study = MAXIFS(AllocationSweep[SweepCorrectedGap], AllocationSweep[StudyId], SweepStudyId). See CorrectedGapConstant for why this is materialized rather than calculated." .

effortless-ntwf:sweepCorrectedGapMin a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SweepCorrectedGapMin" ;
    rdfs:comment "Principled materialization (refresh_sweep_study_summary()). Minimum SweepCorrectedGap across all allocation fractions for this study = MINIFS(AllocationSweep[SweepCorrectedGap], AllocationSweep[StudyId], SweepStudyId). See CorrectedGapConstant for why this is materialized rather than calculated." .

effortless-ntwf:sweepCorrectedGapRange a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SweepCorrectedGapRange" ;
    rdfs:comment "Principled materialization (refresh_sweep_study_summary()). Range of SweepCorrectedGap across the sweep = SweepCorrectedGapMax - SweepCorrectedGapMin. Should be effectively zero (< 0.0001) for all studies if CorrectedGap is truly allocation-free. See CorrectedGapConstant for why this is materialized rather than calculated." .

effortless-ntwf:sweepPooledGapMax a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SweepStudySummary ;
    rdfs:range xsd:string ;
    rdfs:label "SweepPooledGapMax" ;
    rdfs:comment "Principled materialization (refresh_sweep_study_summary()). Maximum SweepPooledGap across all allocation fractions for this study = MAXIFS(AllocationSweep[SweepPooledGap], AllocationSweep[StudyId], SweepStudyId). See CorrectedGapConstant for why this is materialized rather than calculated." .

effortless-ntwf:sweepPooledGapMin a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SweepStudySummary ;
    rdfs:range xsd:string ;
    rdfs:label "SweepPooledGapMin" ;
    rdfs:comment "Principled materialization (refresh_sweep_study_summary()). Minimum SweepPooledGap across all allocation fractions for this study = MINIFS(AllocationSweep[SweepPooledGap], AllocationSweep[StudyId], SweepStudyId). See CorrectedGapConstant for why this is materialized rather than calculated." .

effortless-ntwf:sweepPooledGapRange a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SweepPooledGapRange" ;
    rdfs:comment "Principled materialization (refresh_sweep_study_summary()). Range of SweepPooledGap across the sweep = SweepPooledGapMax - SweepPooledGapMin. Should be large for Type A/B studies (pooled gap wanders) and smaller for Type D (balanced allocation = less wandering). See CorrectedGapConstant for why this is materialized rather than calculated." .

effortless-ntwf:pooledGapCrossesZero a owl:DatatypeProperty ;
    rdfs:range xsd:boolean ;
    rdfs:label "PooledGapCrossesZero" ;
    rdfs:comment "Principled materialization (refresh_sweep_study_summary()). TRUE when SweepPooledGapMin < 0 and SweepPooledGapMax > 0 — the pooled gap changes sign somewhere in the sweep, meaning the paradox can be created or destroyed by pure allocation. See CorrectedGapConstant for why this is materialized rather than calculated." .

effortless-ntwf:invariantWitness a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SweepStudySummary ;
    rdfs:range xsd:string ;
    rdfs:label "InvariantWitness" ;
    rdfs:comment "Summary witness statement: PASS if CorrectedGap is invariant (range < 0.0001), FAIL otherwise." .
    # calculated: calculated

effortless-ntwf:sweepStratumLabel a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SweepStratumLabel" ;
    rdfs:comment "Which stratum is being swept for this study." .
    # calculated: calculated

# === Class: SyntheticPhase ===
effortless-ntwf:SyntheticPhase a owl:Class ;
    rdfs:label "SyntheticPhase" ;
    rdfs:comment "Table: SyntheticPhase — a parametric grid of canonical 2×2 synthetic studies at controlled (ParamStratumFraction, ParamStratumGap1, ParamStratumGap2, ParamAllocationBias) coordinates. Stratum 1 is the hard stratum (base rate 0.45); stratum 2 is easy (base rate 0.75). Each row constructs cell counts from the parameters and derives PhaseDistortionType using the same geometric classification as TreatmentRankings. This maps the A/B/C+/C-/D phase boundaries algebraically — independent of empirical corpus data." .

effortless-ntwf:phaseId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseId" ;
    rdfs:comment "Unique grid-point identifier encoding (w, g1, g2, b) coordinates." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:paramStratumFraction a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "ParamStratumFraction" ;
    rdfs:comment "w — fraction of total cases in the hard (stratum-1) subgroup. Range 0.30–0.75 in this grid." .

effortless-ntwf:paramStratumGap1 a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "ParamStratumGap1" ;
    rdfs:comment "g1 — signed treatment advantage (rate_A − rate_B) in the hard stratum. Positive means A wins stratum 1." .

effortless-ntwf:paramStratumGap2 a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "ParamStratumGap2" ;
    rdfs:comment "g2 — signed treatment advantage in the easy stratum. Negative g2 enables type-B (partial reversal) cells." .

effortless-ntwf:paramAllocationBias a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "ParamAllocationBias" ;
    rdfs:comment "b — allocation bias in stratum 1: extra fraction of stratum-1 cases assigned to treatment A above 50/50. Range 0.00–0.95." .

effortless-ntwf:phaseS1Total a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseS1Total" ;
    rdfs:comment "Total cases in stratum 1 = ROUND(200 × ParamStratumFraction)." .
    # calculated: calculated

effortless-ntwf:phaseNA1 a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseNA1" ;
    rdfs:comment "Treatment-A cases in stratum 1." .
    # calculated: calculated

effortless-ntwf:phaseNB1 a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseNB1" ;
    rdfs:comment "Treatment-B cases in stratum 1." .
    # calculated: calculated

effortless-ntwf:phaseNA2 a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseNA2" ;
    rdfs:comment "Treatment-A cases in stratum 2." .
    # calculated: calculated

effortless-ntwf:phaseNB2 a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseNB2" ;
    rdfs:comment "Treatment-B cases in stratum 2." .
    # calculated: calculated

effortless-ntwf:phaseRateA1 a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseRateA1" ;
    rdfs:comment "Success rate for A in hard stratum." .
    # calculated: calculated

effortless-ntwf:phaseRateB1 a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseRateB1" ;
    rdfs:comment "Success rate for B in hard stratum." .
    # calculated: calculated

effortless-ntwf:phaseRateA2 a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseRateA2" ;
    rdfs:comment "Success rate for A in easy stratum." .
    # calculated: calculated

effortless-ntwf:phaseRateB2 a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseRateB2" ;
    rdfs:comment "Success rate for B in easy stratum." .
    # calculated: calculated

effortless-ntwf:phasePooledRateA a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "PhasePooledRateA" ;
    rdfs:comment "Pooled success rate for treatment A." .
    # calculated: calculated

effortless-ntwf:phasePooledRateB a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "PhasePooledRateB" ;
    rdfs:comment "Pooled success rate for treatment B." .
    # calculated: calculated

effortless-ntwf:phaseSignedPooledGap a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseSignedPooledGap" ;
    rdfs:comment "Pooled rate gap (A minus B)." .
    # calculated: calculated

effortless-ntwf:phaseCorrectedGap a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseCorrectedGap" ;
    rdfs:comment "Allocation-free equal-weight gap = w×g1 + (1−w)×g2." .
    # calculated: calculated

effortless-ntwf:phaseStrataWonByLoser a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseStrataWonByLoser" ;
    rdfs:comment "Count of strata whose per-stratum winner disagrees with the pooled winner." .
    # calculated: calculated

effortless-ntwf:phaseReversalIntensity a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseReversalIntensity" ;
    rdfs:comment "PhaseStrataWonByLoser / 2 for this 2-stratum template." .
    # calculated: calculated

effortless-ntwf:phaseIsSignFlip a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:boolean ;
    rdfs:label "PhaseIsSignFlip" ;
    rdfs:comment "TRUE when equal-weight and pooled gaps have opposite signs." .
    # calculated: calculated

effortless-ntwf:phaseAllocationDistortion a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseAllocationDistortion" ;
    rdfs:comment "|PhaseCorrectedGap − PhaseSignedPooledGap|." .
    # calculated: calculated

effortless-ntwf:phaseDistortionType a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:SyntheticPhase ;
    rdfs:range xsd:string ;
    rdfs:label "PhaseDistortionType" ;
    rdfs:comment "Five-type geometric classification at this parameter point." .
    # calculated: calculated

# === Class: TreatmentRankings ===
effortless-ntwf:TreatmentRankings a owl:Class ;
    rdfs:label "TreatmentRankings" ;
    rdfs:comment "Table: TreatmentRankings — one row per treatment-pair comparison within a study. Derives the pooled winner, the per-stratum winner, and the IsReversal boolean. The paradox emerges here as a derived fact: IsReversal is true when the pooled winner and the per-stratum winner disagree." .

effortless-ntwf:treatmentRankingId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "TreatmentRankingId" ;
    rdfs:comment "Unique slug (e.g. kidney-1986-A-vs-B)." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:study a owl:ObjectProperty ;
    rdfs:range effortless-ntwf:Studies ;
    rdfs:label "Study" ;
    rdfs:comment "FK to the parent Study." .

effortless-ntwf:treatmentA a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "TreatmentA" ;
    rdfs:comment "Label for the first treatment in the comparison (matches Treatments.TreatmentLabel)." .

effortless-ntwf:treatmentB a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "TreatmentB" ;
    rdfs:comment "Label for the second treatment in the comparison." .

effortless-ntwf:totalCasesA a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:integer ;
    rdfs:label "TotalCasesA" ;
    rdfs:comment "Total cases for TreatmentA across all strata in this study." .
    # calculated: aggregation

effortless-ntwf:totalSuccessesA a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:integer ;
    rdfs:label "TotalSuccessesA" ;
    rdfs:comment "Total successes for TreatmentA across all strata." .
    # calculated: aggregation

effortless-ntwf:pooledRateA a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "PooledRateA" ;
    rdfs:comment "Pooled success rate for TreatmentA: TotalSuccessesA / TotalCasesA." .
    # calculated: calculated

effortless-ntwf:totalCasesB a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:integer ;
    rdfs:label "TotalCasesB" ;
    rdfs:comment "Total cases for TreatmentB across all strata." .
    # calculated: aggregation

effortless-ntwf:totalSuccessesB a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:integer ;
    rdfs:label "TotalSuccessesB" ;
    rdfs:comment "Total successes for TreatmentB across all strata." .
    # calculated: aggregation

effortless-ntwf:pooledRateB a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "PooledRateB" ;
    rdfs:comment "Pooled success rate for TreatmentB: TotalSuccessesB / TotalCasesB." .
    # calculated: calculated

effortless-ntwf:pooledWinner a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "PooledWinner" ;
    rdfs:comment "Which treatment wins when strata are ignored. Returns tie when pooled rates are equal within floating-point tolerance." .
    # calculated: calculated

effortless-ntwf:stratumCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:integer ;
    rdfs:label "StratumCount" ;
    rdfs:comment "Number of strata in this study. Counted from StratumSummaries filtered to TreatmentA rows (one row per stratum)." .
    # calculated: aggregation

effortless-ntwf:strataWonByA a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:integer ;
    rdfs:label "StrataWonByA" ;
    rdfs:comment "Number of strata where A wins: count StratumSummaries rows whose TreatmentLabel=TreatmentA AND StratumWinner=TreatmentA. These are the one-per-stratum sentinel rows." .
    # calculated: aggregation

effortless-ntwf:strataWonByB a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:integer ;
    rdfs:label "StrataWonByB" ;
    rdfs:comment "Number of strata where B wins: count StratumSummaries rows whose TreatmentLabel=TreatmentA AND StratumWinner=TreatmentB." .
    # calculated: aggregation

effortless-ntwf:perStratumWinner a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "PerStratumWinner" ;
    rdfs:comment "The treatment that wins in every stratum — 'A' if StrataWonByA = StratumCount, 'B' if StrataWonByB = StratumCount, else 'none' (no unanimous per-stratum winner)." .
    # calculated: calculated

effortless-ntwf:isStratumUnanimous a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:boolean ;
    rdfs:label "IsStratumUnanimous" ;
    rdfs:comment "TRUE when one treatment wins every stratum (StrataWonByA = StratumCount OR StrataWonByB = StratumCount). Maps A/B policy split: unanimous → stratify-immediately geometry; heterogeneous → investigate-confounder." .
    # calculated: calculated

effortless-ntwf:isReversal a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:boolean ;
    rdfs:label "IsReversal" ;
    rdfs:comment "TRUE when the equal-weight pooled signal (WeightedStratumGapSum) and the actual pooled signal (SignedPooledGap) point in opposite directions — allocation has flipped the direction of the aggregate conclusion. This is Simpson's Paradox as a derived fact." .
    # calculated: calculated

effortless-ntwf:confoundersInStudy a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:integer ;
    rdfs:label "ConfoundersInStudy" ;
    rdfs:comment "Count of StratumVariables in this study whose IsConfounder = TRUE. When > 0, the paradox has a causal explanation." .
    # calculated: aggregation

effortless-ntwf:isParadoxExplained a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:boolean ;
    rdfs:label "IsParadoxExplained" ;
    rdfs:comment "TRUE when IsReversal is present AND at least one confirmed confounder exists in the study." .
    # calculated: calculated

effortless-ntwf:pooledGap a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "PooledGap" ;
    rdfs:comment "Absolute difference between the two pooled rates: |PooledRateA - PooledRateB|. The size of the aggregate misleading signal." .
    # calculated: calculated

effortless-ntwf:strataWonByLoser a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:integer ;
    rdfs:label "StrataWonByLoser" ;
    rdfs:comment "Number of strata won by the pooled loser — the counter-signal. For full reversals this equals StratumCount; for partial paradoxes it is between 0 and StratumCount." .
    # calculated: calculated

effortless-ntwf:paradoxStrength a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "ParadoxStrength" ;
    rdfs:comment "Scalar severity of the paradox: PooledGap × (StrataWonByLoser / StratumCount). Zero when no strata go against the pooled winner. Positive for partial paradoxes. Maximum when every stratum contradicts the pooled result." .
    # calculated: calculated

effortless-ntwf:pooledRateFromWeightsA a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "PooledRateFromWeightsA" ;
    rdfs:comment "Pooled rate for TreatmentA reconstructed as a weighted average of stratum rates: SUM(WeightedStratumRate) across all stratum rows for TreatmentA. Must match PooledRateA — this witnesses the mechanism equation: the pooled rate IS a weighted average of stratum rates, weighted by StratumFraction." .
    # calculated: aggregation

effortless-ntwf:pooledRateFromWeightsB a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "PooledRateFromWeightsB" ;
    rdfs:comment "Pooled rate for TreatmentB reconstructed as a weighted average. Must match PooledRateB. If it does, the mechanism equation is verified: reversal is purely a consequence of differential weighting." .
    # calculated: aggregation

effortless-ntwf:reversalIntensity a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "ReversalIntensity" ;
    rdfs:comment "StrataWonByLoser / StratumCount: the fraction of strata that go against the pooled winner. Zero for no paradox, 1.0 for full reversal, between 0 and 1 for partial. This is the allocation-side measure of how deeply the confounding has penetrated the stratified view." .
    # calculated: calculated

effortless-ntwf:thresholdMargin a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "ThresholdMargin" ;
    rdfs:comment "ReversalIntensity minus 0.5: positive when more than half the strata go against the pooled winner (reversal is robust), zero at the tipping point (exactly half), negative when fewer than half oppose the pooled winner (reversal is fragile or absent). A study with ThresholdMargin > 0 is robustly paradoxical; one with ThresholdMargin < 0 has a weak or absent paradox." .
    # calculated: calculated

effortless-ntwf:signedPooledGap a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "SignedPooledGap" ;
    rdfs:comment "Signed difference between pooled rates: PooledRateA minus PooledRateB. Positive = A wins pooled; negative = B wins pooled. Unlike PooledGap (which is absolute), this preserves direction — essential for detecting sign flips between the equal-weight and actual pooled signals." .
    # calculated: calculated

effortless-ntwf:weightedStratumGapSum a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "WeightedStratumGapSum" ;
    rdfs:comment "Sum of WeightedStratumGap (= StratumGap × StratumFraction) across all StratumSummaries rows where TreatmentLabel = TreatmentA for this study. This is the equal-weight pooled gap — the gap the pooled analysis would produce if allocation were perfectly balanced across strata. When this has the opposite sign to SignedPooledGap, the allocation has not merely compressed the signal: it has flipped it entirely." .
    # calculated: aggregation

effortless-ntwf:isSignFlip a owl:DatatypeProperty ;
    rdfs:range xsd:boolean ;
    rdfs:label "IsSignFlip" ;
    rdfs:comment "TRUE when WeightedStratumGapSum and SignedPooledGap have opposite signs. Same condition as IsReversal; retained as the coordinate-level name in the DAG." .
    # calculated: calculated

effortless-ntwf:allocationDistortion a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "AllocationDistortion" ;
    rdfs:comment "The magnitude of how far the allocation has bent the pooled signal: |WeightedStratumGapSum − SignedPooledGap|. Zero means the allocation is neutral — the pooled analysis faithfully represents the equal-weight stratum evidence. A large AllocationDistortion means the allocation is doing most of the work: the pooled number is mostly noise from how cases were assigned, not signal about which treatment is better. This measure does NOT require a sign flip to be nonzero — it captures any allocation-induced distortion, not just reversals." .
    # calculated: calculated

effortless-ntwf:distortionType a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "DistortionType" ;
    rdfs:comment "Geometric classification (five types A/B/C+/C-/D). Sign-flip types: A = unanimous stratum direction (IsStratumUnanimous); B = heterogeneous stratum directions. Non-flip: C+/C-/D by distortion ratio." .
    # calculated: calculated

effortless-ntwf:policyImplication a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "PolicyImplication" ;
    rdfs:comment "The researcher action implied by the geometric classification. Derived from DistortionType: type A (full reversal) → 'stratify-immediately' — the pooled conclusion is directionally wrong; do not act on it without stratifying. Type B (partial sign-flip) → 'investigate-confounder' — the equal-weight signal opposes the pooled signal, but the per-stratum picture is mixed; the causal mechanism needs examination before trusting either estimate. Type C (compression without flip) → 'check-allocation-bias' — the pooled winner is probably correct but the effect size is distorted; report adjusted or standardized margins rather than the raw pooled gap. Type D (neutral) → 'pooled-analysis-trustworthy' — allocation is not materially distorting the signal; the pooled conclusion is safe with respect to this stratification." .
    # calculated: calculated

effortless-ntwf:correctedGap a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "CorrectedGap" ;
    rdfs:comment "The allocation-corrected treatment gap: WeightedStratumGapSum, i.e. what SignedPooledGap would be if allocation were equal across strata. Positive means A is favoured in the corrected world; negative means B is favoured." .
    # calculated: calculated

effortless-ntwf:correctedWinner a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "CorrectedWinner" ;
    rdfs:comment "Which treatment wins once allocation bias is removed. Derived from CorrectedGap: positive → TreatmentA; negative → TreatmentB; near-zero → tie." .
    # calculated: calculated

effortless-ntwf:correctedVsPooledAgreement a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:boolean ;
    rdfs:label "CorrectedVsPooledAgreement" ;
    rdfs:comment "TRUE when the allocation-corrected winner matches the pooled winner. FALSE when removing allocation bias would change which treatment appears to win — the machine-readable definition of a reversal recovery." .
    # calculated: calculated

effortless-ntwf:correctedPolicyImplication a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "CorrectedPolicyImplication" ;
    rdfs:comment "The allocation-aware researcher action: what to do when the corrected verdict is available. Derived from DistortionType and CorrectedWinner together, making the instrument self-consistent with Reversal Recovery (loop-27). Type A (full sign-flip, unanimous per-stratum reversal): CorrectedWinner is the true signal — 'use-corrected-winner'. The pooled analysis was directionally wrong; act on CorrectedWinner, not PooledWinner. Type B (partial sign-flip, non-unanimous): CorrectedWinner points against the pooled signal but strata disagree among themselves — 'use-corrected-winner-with-caution'. Trust the corrected direction but acknowledge residual uncertainty. Type C (compression, no sign flip): PooledWinner is directionally correct; the distortion is in the magnitude, not the direction — 'check-allocation-bias'. Same as PolicyImplication. Type D (neutral): allocation is not materially distorting the signal — 'pooled-analysis-trustworthy'. Same as PolicyImplication. For Types C and D, CorrectedPolicyImplication and PolicyImplication always agree; for Types A and B, CorrectedPolicyImplication supersedes PolicyImplication with the allocation-corrected verdict." .
    # calculated: calculated

effortless-ntwf:allocationDirection a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "AllocationDirection" ;
    rdfs:comment "Whether the allocation is reversing, amplifying, or compressing the true treatment signal (CorrectedGap). 'reversal': allocation changed the sign of the pooled gap relative to CorrectedGap — worst case; pooled winner is wrong (types A and B). 'amplification': allocation inflated the pooled gap beyond CorrectedGap in the same direction — pooled winner is correct but effect size is overstated (type C+). 'compression': allocation shrank the pooled gap below CorrectedGap in the same direction — pooled winner is correct but effect size is understated (type C-). 'neutral': allocation had negligible effect on the pooled gap (type D). Policy consequences: reversal → wrong decision; amplification → overconfidence; compression → underconfidence / premature abandonment; neutral → trustworthy." .
    # calculated: calculated

effortless-ntwf:signalPurity a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SignalPurity" ;
    rdfs:comment "The fraction of total observed variation (|CorrectedGap| + AllocationDistortion) that is genuine treatment signal rather than allocation noise. SignalPurity = |CorrectedGap| / (|CorrectedGap| + AllocationDistortion). Range [0,1]: 1.0 = allocation contributed nothing (type D); 0 = corrected gap is zero (pure noise). Critical threshold is 0.5: SignalPurity < 0.5 means allocation noise exceeds true signal. The signal-purity theorem (loop-44): AllocationDirection='reversal' → SignalPurity < 0.5. Equivalently: a sign flip requires the confound to produce more distortion than the treatment produced signal. This is the mechanistic account of why reversals happen: not study-design accidents but algebraic necessities." .
    # calculated: calculated

effortless-ntwf:pooledGapCrossesZero a owl:DatatypeProperty ;
    rdfs:range xsd:boolean ;
    rdfs:label "PooledGapCrossesZero" ;
    rdfs:comment "Lookup: SweepStudySummary.PooledGapCrossesZero via Study. TRUE when reweighting allocation would change the sign of the pooled gap." .
    # calculated: calculated

effortless-ntwf:sweepPooledGapRange a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "SweepPooledGapRange" ;
    rdfs:comment "Lookup: SweepStudySummary.SweepPooledGapRange via Study. Width of pooled-gap wander under allocation sweep." .
    # calculated: calculated

effortless-ntwf:latentFlipPotential a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:boolean ;
    rdfs:label "LatentFlipPotential" ;
    rdfs:comment "TRUE when DistortionType=D but PooledGapCrossesZero=TRUE — SAFE at observed allocation but flip-capable under counterfactual reweighting." .
    # calculated: calculated

effortless-ntwf:allocationFragility a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "AllocationFragility" ;
    rdfs:comment "SweepPooledGapRange divided by |PooledGap|. High values mean small observed effect but wide counterfactual wander. Fixed loop-90: this rulebook's IF(x=\"\",\"\",...) blank-on-degenerate idiom transpiles to a bare ''::numeric cast that errors at runtime the moment a live row genuinely has PooledGap=0 (surfaced only once _erb_tr_metrics, which masked this row, was eliminated). Returns 0 for the degenerate case instead — see TreatmentRankings.DistortionRatio for the same fix pattern." .
    # calculated: calculated

effortless-ntwf:maxStratumImbalance a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "MaxStratumImbalance" ;
    rdfs:comment "MAXIFS(StratumSummaries.AbsAllocationBias) over rows where Study matches and TreatmentLabel='A' — the largest allocation-share skew any single stratum shows for this study. Live aggregation; replaces the former hand-written _erb_tr_metrics cache column of the same name." .
    # calculated: aggregation

effortless-ntwf:maxStratumGap a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "MaxStratumGap" ;
    rdfs:comment "MAXIFS(StratumSummaries.AbsStratumGap) over rows where Study matches and TreatmentLabel='A' — the largest within-stratum rate gap for this study. Live aggregation; replaces the former hand-written _erb_tr_metrics cache column of the same name." .
    # calculated: aggregation

effortless-ntwf:isSweepFragile a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:boolean ;
    rdfs:label "IsSweepFragile" ;
    rdfs:comment "Type-D study that is pooled-safe but ultra-fragile under allocation reweighting: signal_purity=1, allocation_distortion=0, sweep_pooled_gap_range>0.3, allocation_fragility>=10." .
    # calculated: calculated

effortless-ntwf:studyDomain a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "StudyDomain" ;
    rdfs:comment "Lookup: Studies.Domain via Study." .
    # calculated: calculated

effortless-ntwf:stratumCausalRole a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "StratumCausalRole" ;
    rdfs:comment "Lookup: StratumVariables.CausalRole for this study — confounder, mediator, collider, selection, contested, etc." .
    # calculated: lookup

effortless-ntwf:isLatentOnlyFlip a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:boolean ;
    rdfs:label "IsLatentOnlyFlip" ;
    rdfs:comment "TRUE when allocation sweep crosses zero but observed allocation shows no sign flip — latent-only reversal." .
    # calculated: calculated

effortless-ntwf:confirmedCausalRoleCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:integer ;
    rdfs:label "ConfirmedCausalRoleCount" ;
    rdfs:comment "Count of StratumVariables in this study whose CausalRole is \"confounder\" — includes variables that confound the outcome regardless of whether allocation was balanced." .
    # calculated: aggregation

effortless-ntwf:mediatorRiskCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:integer ;
    rdfs:label "MediatorRiskCount" ;
    rdfs:comment "Count of StratumVariables in this study whose ConditioningRisk is mediator or collider — the hazardous conditioning types." .
    # calculated: aggregation

effortless-ntwf:contestedStratumCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:integer ;
    rdfs:label "ContestedStratumCount" ;
    rdfs:comment "Count of StratumVariables in this study whose CausalRole is contested — the causal interpretation is disputed." .
    # calculated: aggregation

effortless-ntwf:unknownCausalRoleCount a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:integer ;
    rdfs:label "UnknownCausalRoleCount" ;
    rdfs:comment "Count of StratumVariables in this study whose CausalRole is unknown — no causal account has been established." .
    # calculated: aggregation

effortless-ntwf:causalClaimStatus a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "CausalClaimStatus" ;
    rdfs:comment "The epistemic status of the causal interpretation for this study. Values: established (all stratum variables are confirmed confounders with known mechanisms), contested (at least one stratum variable has a disputed causal role — e.g. Berkeley 1973 where department choice may be endogenous), geometric-only (no stratum variable has an established causal role — the distortion is a geometric fact, not a causal claim). Derived from StratumVariables.CausalRole and ConditioningRisk." .
    # calculated: calculated

effortless-ntwf:adjustmentAppropriate a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:boolean ;
    rdfs:label "AdjustmentAppropriate" ;
    rdfs:comment "TRUE only when it is epistemically safe to act on CorrectedGap as the causal treatment effect estimate: no mediator/collider conditioning risk AND causal claim is established (not contested or geometric-only). FALSE for the birth-weight paradox (mediator), berkeley-1973 (contested), and synthetic/degenerate studies (geometric-only)." .
    # calculated: calculated

effortless-ntwf:adjustmentRationale a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "AdjustmentRationale" ;
    rdfs:comment "Human-authored note explaining why AdjustmentAppropriate is TRUE or FALSE for this study. Covers mediator/collider hazards, contested causal roles, and cases where geometric correction is valid but causal interpretation is uncertain." .

effortless-ntwf:purityTrapFlag a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:boolean ;
    rdfs:label "PurityTrapFlag" ;
    rdfs:comment "TRUE when SignalPurity >= 0.90 AND the study's confounder identity has PolicyDefault IN ('do-not-condition','do-not-naively-adjust'). Marks the purity trap: data appears cleanest where naive stratification is most harmful." .

effortless-ntwf:distortionRatio a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "DistortionRatio" ;
    rdfs:comment "SignedPooledGap / WeightedStratumGapSum. Promoted loop-90 from a 03b-customize-views.sql presentation-layer bolt-on to a first-class rulebook field. The IF(x=0,\"\",...) blank-on-zero-denominator idiom used elsewhere in this rulebook is unsafe here: NULLIF is not a supported transpiler function, and the transpiler's translation of the \"\" branch for a numeric-typed field is a bare ''::numeric cast that errors at runtime the moment a live row actually hits it (4 of 242 TreatmentRankings rows have WeightedStratumGapSum=0). Returns 0 in that case instead — WeightedStratumGapSum=0 means there is no equal-weight baseline signal to compare against, so a 0 ratio (no distortion multiplier) is the defensible convention, not a guess." .
    # calculated: calculated

effortless-ntwf:screeningTier a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "ScreeningTier" ;
    rdfs:comment "DANGER when DistortionType is A or B, CAUTION when C+/C-, SAFE when D. Promoted loop-90 (see DistortionRatio)." .
    # calculated: calculated

effortless-ntwf:armSizeRatio a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "ArmSizeRatio" ;
    rdfs:comment "TotalCasesA / (TotalCasesA + TotalCasesB) — the share of total cases allocated to TreatmentA. Promoted loop-90 (see DistortionRatio for why the empty-string zero-denominator idiom is unsafe on a numeric field). Returns 0 for the degenerate zero-case-count edge (never occurs in this corpus, kept for defensive symmetry with DistortionRatio)." .
    # calculated: calculated

effortless-ntwf:symmetryDeparture a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:TreatmentRankings ;
    rdfs:range xsd:string ;
    rdfs:label "SymmetryDeparture" ;
    rdfs:comment "ABS(ArmSizeRatio - 0.5) — how far the case allocation departs from a balanced 50/50 split. Promoted loop-90 (see DistortionRatio)." .
    # calculated: calculated

# === Class: Treatments ===
effortless-ntwf:Treatments a owl:Class ;
    rdfs:label "Treatments" ;
    rdfs:comment "Table: Treatments — the interventions being compared within a study. Each treatment belongs to one study." .

effortless-ntwf:treatmentId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Treatments ;
    rdfs:range xsd:string ;
    rdfs:label "TreatmentId" ;
    rdfs:comment "Unique slug (e.g. kidney-1986-A, kidney-1986-B)." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label." .
    # calculated: calculated

effortless-ntwf:study a owl:ObjectProperty ;
    rdfs:range effortless-ntwf:Studies ;
    rdfs:label "Study" ;
    rdfs:comment "FK to the parent Study." .

effortless-ntwf:treatmentLabel a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TreatmentLabel" ;
    rdfs:comment "Short label used in CaseCells and comparisons (e.g. A, B)." .

effortless-ntwf:description a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Description" ;
    rdfs:comment "Plain-language description of what this treatment is." .

effortless-ntwf:totalCases a owl:DatatypeProperty ;
    rdfs:range xsd:integer ;
    rdfs:label "TotalCases" ;
    rdfs:comment "Total cases for this treatment across all strata." .
    # calculated: aggregation

effortless-ntwf:totalSuccesses a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Treatments ;
    rdfs:range xsd:integer ;
    rdfs:label "TotalSuccesses" ;
    rdfs:comment "Total successes for this treatment across all strata." .
    # calculated: aggregation

effortless-ntwf:pooledSuccessRate a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:Treatments ;
    rdfs:range xsd:string ;
    rdfs:label "PooledSuccessRate" ;
    rdfs:comment "Pooled (across all strata) success rate for this treatment: TotalSuccesses / TotalCases." .
    # calculated: calculated

# === Class: UIComponents ===
effortless-ntwf:UIComponents a owl:Class ;
    rdfs:label "UIComponents" ;
    rdfs:comment "Table: UIComponents — the reusable visual building blocks used across UIScreens. Each row defines one component: what it renders, which entity it reads, and what props it requires. A developer can implement the full UI by composing these components onto the screens defined in UIScreens." .

effortless-ntwf:componentId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:UIComponents ;
    rdfs:range xsd:string ;
    rdfs:label "ComponentId" ;
    rdfs:comment "Unique slug (e.g. comp-01-distortion-badge)." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label, mirrors ComponentId." .
    # calculated: calculated

effortless-ntwf:componentName a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:UIComponents ;
    rdfs:range xsd:string ;
    rdfs:label "ComponentName" ;
    rdfs:comment "Code-level component name (PascalCase, e.g. DistortionBadge)." .

effortless-ntwf:usedOnScreens a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:UIComponents ;
    rdfs:range xsd:string ;
    rdfs:label "UsedOnScreens" ;
    rdfs:comment "Comma-separated ScreenIds where this component appears." .

effortless-ntwf:primaryEntity a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "PrimaryEntity" ;
    rdfs:comment "The rulebook entity this component reads (the vw_* view)." .

effortless-ntwf:description a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Description" ;
    rdfs:comment "What this component renders and what decisions a developer must make to implement it." .

effortless-ntwf:requiredFields a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "RequiredFields" ;
    rdfs:comment "Comma-separated rulebook fields this component must receive as props." .

effortless-ntwf:visualSpec a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:UIComponents ;
    rdfs:range xsd:string ;
    rdfs:label "VisualSpec" ;
    rdfs:comment "Plain-English description of the visual output: shape, color coding, interactivity." .

# === Class: UIScreens ===
effortless-ntwf:UIScreens a owl:Class ;
    rdfs:label "UIScreens" ;
    rdfs:comment "Table: UIScreens — the canonical screens that comprise the Simpson's Paradox Explorer UI. Each row defines one screen: its purpose, its primary data source (which rulebook view it reads), and the user action it supports. A developer can reconstruct the full UI from this table without any prior knowledge of the application." .

effortless-ntwf:screenId a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:UIScreens ;
    rdfs:range xsd:string ;
    rdfs:label "ScreenId" ;
    rdfs:comment "Unique slug (e.g. screen-01-study-list)." .

effortless-ntwf:name a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Name" ;
    rdfs:comment "Display label, mirrors ScreenId." .
    # calculated: calculated

effortless-ntwf:title a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "Title" ;
    rdfs:comment "Human-readable screen title shown in the UI." .

effortless-ntwf:route a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:UIScreens ;
    rdfs:range xsd:string ;
    rdfs:label "Route" ;
    rdfs:comment "URL path pattern (e.g. /, /study/:studyId, /compare)." .

effortless-ntwf:primaryEntity a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "PrimaryEntity" ;
    rdfs:comment "The main rulebook entity this screen reads from (the vw_* view name without prefix)." .

effortless-ntwf:purpose a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:UIScreens ;
    rdfs:range xsd:string ;
    rdfs:label "Purpose" ;
    rdfs:comment "One sentence: what a user can learn or do on this screen." .

effortless-ntwf:keyFields a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:UIScreens ;
    rdfs:range xsd:string ;
    rdfs:label "KeyFields" ;
    rdfs:comment "Comma-separated list of the most important fields displayed on this screen." .

effortless-ntwf:userAction a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:UIScreens ;
    rdfs:range xsd:string ;
    rdfs:label "UserAction" ;
    rdfs:comment "Primary user action available on this screen (e.g. select a study, drill into a stratum)." .

effortless-ntwf:traditionId a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionId" ;
    rdfs:comment "FK → ResearchTraditions.TraditionId. The scholarly tradition whose lens this screen primarily serves — determines which framing language and which fields are most prominent." .

effortless-ntwf:primaryConclusion a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:UIScreens ;
    rdfs:range xsd:string ;
    rdfs:label "PrimaryConclusion" ;
    rdfs:comment "FK → Conclusions.ConclusionId. The conclusion this screen most directly visualises or communicates to the user." .

effortless-ntwf:traditionName a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionName" ;
    rdfs:comment "Lookup: ResearchTraditions.Name via TraditionId." .
    # calculated: lookup

effortless-ntwf:traditionCoreConern a owl:DatatypeProperty ;
    rdfs:range xsd:string ;
    rdfs:label "TraditionCoreConern" ;
    rdfs:comment "Lookup: ResearchTraditions.CoreConcern via TraditionId — the lens this screen applies." .
    # calculated: lookup

effortless-ntwf:primaryConclusionTitle a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:UIScreens ;
    rdfs:range xsd:string ;
    rdfs:label "PrimaryConclusionTitle" ;
    rdfs:comment "Lookup: Conclusions.Title via PrimaryConclusion." .
    # calculated: lookup

effortless-ntwf:primaryConclusionEvidence a owl:DatatypeProperty ;
    rdfs:domain effortless-ntwf:UIScreens ;
    rdfs:range xsd:string ;
    rdfs:label "PrimaryConclusionEvidence" ;
    rdfs:comment "Lookup: Conclusions.Evidence via PrimaryConclusion." .
    # calculated: lookup
