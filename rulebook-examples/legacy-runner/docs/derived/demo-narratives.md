<!-- GENERATED FILE — DO NOT EDIT. -->
<!-- Source: effortless-platform/effortless-rulebook/effortless-rulebook.json (table: `DemoNarratives`) -->
<!-- Regenerate with: cd effortless-platform && effortless build -->

# Demo Narratives

Curated narratives that demonstrate ERB capabilities in motion: substrates 'follow along' as the rulebook changes. Two narrative styles coexist: (1) commit-sequence narratives (legacy) where a single demo rulebook evolves over commits; (2) cross-rulebook narratives (current) where the same demo is observed across DIFFERENT rulebooks side-by-side. Cross-rulebook supersedes commit-sequence because RulebookDomains now form a tree (ParentDomainId) — the progression is data, not git history.

## Substrate Drift (BASIC → ADVANCED → NEW ONTOLOGY)

### Step 1: BASIC

**What happens.** All substrates pass at 100% on a 9-table workflow ontology with relationships, aggregations, and boolean derivations. [Referenced demo talisman-basic has been removed from the repo.]

**Key lesson.** Substrates have wildly different regeneration costs even at full conformance.

**Observed cost.** Python/Go/Postgres < 1s, OWL ~10s, English ~5min (~85% LLM-graded)

### Step 2: ADVANCED

**What happens.** Rename and modify elements within the same ontology — add cross-entity lookups and conditional logic. Every substrate follows along with no edits to substrate code. [Referenced demo talisman-advanced has been removed from the repo.]

**Key lesson.** Interface changes propagate automatically. This is the polymorphism payoff.

**Observed cost.** Cost profile unchanged from BASIC — work is in the rulebook, not in substrate code.

### Step 3: NEW ONTOLOGY

_Domain:_ `domain-star-trek`

**What happens.** Swap to an entirely different domain (e.g. star-trek media catalog). Deterministic substrates regenerate and conform. The English substrate is now 100% wrong against the new answer key until its prose is regenerated by the LLM.

**Key lesson.** The static English content is 100% stale across domain swaps. Regenerating it costs time and money — the dominant cost lives in the slowest substrate, not in the rulebook.

**Observed cost.** Deterministic substrates < 1s; English regeneration ~5min and ~$0.10-$1 in LLM tokens.

## Substrate Drift (cross-rulebook progression)

### Step 1: CustomerFullName (root)

_Domain:_ `domain-customer-fullname`

**What happens.** Start at the simplest rulebook in the tree — one table, one CONCAT formula. Every substrate emits its analog. Conformance is 100% across the board.

**Key lesson.** The contract is observable at minimum scale. A 1-table rulebook is enough to exercise inject + execute + grade end-to-end.

**Observed cost.** All substrates < 1s; English regeneration ~30s.

### Step 2: Talisman BASIC (child of root)

**What happens.** Walk down the progression tree to Talisman BASIC — adds relationships, aggregations, role/agent separation. Every substrate handles the elaboration with no substrate-side edits. [Referenced demo talisman-basic has been removed from the repo.]

**Key lesson.** Walking the tree of rulebooks (ParentDomainId) IS the demo — no git rewind required. The viewer can see N rulebooks side-by-side rather than replaying commits.

**Observed cost.** Python/Go/Postgres < 1s, OWL ~10s, English ~5min.

### Step 3: Talisman ADVANCED (child of BASIC)

**What happens.** Walk further down: Talisman ADVANCED elaborates BASIC with cross-entity lookups and conditional IF logic. Conformance still 100% across deterministic substrates. [Referenced demo talisman-advanced has been removed from the repo.]

**Key lesson.** Elaboration within a tree of rulebooks costs the SAME as elaboration within a commit history — proving that 'commits' was never the load-bearing thing; the progression structure was.

**Observed cost.** Cost profile unchanged from BASIC.

### Step 4: Star Trek (sibling root)

_Domain:_ `domain-star-trek`

**What happens.** Jump to a sibling root in the tree — different domain entirely. Deterministic substrates regenerate and conform. The English substrate is 100% stale until regenerated.

**Key lesson.** Cross-rulebook navigation makes the 'domain swap' observable as a tree traversal, not a git checkout. Same lesson as the legacy commit-sequence demo, but now data-driven.

**Observed cost.** Deterministic substrates < 1s; English regeneration ~5min and ~$0.10-$1.

