-- ============================================================================
-- CREATE TABLES (check-add mode) - Idempotent schema, never drops columns
-- ============================================================================
-- Generated by rulebook-to-postgres tool
-- Mode: check-add    Stage: dev
-- Total Tables: 3
-- Total Raw Fields: 11
-- ============================================================================

SET timezone = 'UTC';

-- ============================================================================
-- CREATE TABLES - Normalized data only (no calculated fields)
-- ============================================================================

-- ----------------------------------------------------------------------------
-- GateTypes: Table: GateTypes
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS gate_types (
  gate_id                             TEXT                 PRIMARY KEY          -- GateId
);
ALTER TABLE gate_types ADD COLUMN IF NOT EXISTS symbol TEXT;                                        -- Human-readable gate symbol
ALTER TABLE gate_types ADD COLUMN IF NOT EXISTS description TEXT;                                   -- What the gate does
COMMENT ON COLUMN gate_types.gate_id IS 'GateId';
COMMENT ON COLUMN gate_types.symbol IS 'Human-readable gate symbol';
COMMENT ON COLUMN gate_types.description IS 'What the gate does';

-- ----------------------------------------------------------------------------
-- GateTruthRows: Table: GateTruthRows
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS gate_truth_rows (
  truth_row_id                        TEXT                 PRIMARY KEY          -- gate|in0|in1 -- the PK IS the lookup key
);
ALTER TABLE gate_truth_rows ADD COLUMN IF NOT EXISTS truth_row_name TEXT;                           -- Readable truth-table line
ALTER TABLE gate_truth_rows ADD COLUMN IF NOT EXISTS gate TEXT;                                     -- Which gate
ALTER TABLE gate_truth_rows ADD COLUMN IF NOT EXISTS in0 INTEGER;                                   -- Left input bit
ALTER TABLE gate_truth_rows ADD COLUMN IF NOT EXISTS in1 INTEGER;                                   -- Right input bit
ALTER TABLE gate_truth_rows ADD COLUMN IF NOT EXISTS out_bit INTEGER;                               -- Output bit for this input pair
COMMENT ON COLUMN gate_truth_rows.truth_row_id IS 'gate|in0|in1 -- the PK IS the lookup key';
COMMENT ON COLUMN gate_truth_rows.truth_row_name IS 'Readable truth-table line';
COMMENT ON COLUMN gate_truth_rows.gate IS 'Which gate';
COMMENT ON COLUMN gate_truth_rows.in0 IS 'Left input bit';
COMMENT ON COLUMN gate_truth_rows.in1 IS 'Right input bit';
COMMENT ON COLUMN gate_truth_rows.out_bit IS 'Output bit for this input pair';

-- ----------------------------------------------------------------------------
-- Wires: Table: Wires
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS wires (
  wire_id                             TEXT                 PRIMARY KEY          -- WireId
);
ALTER TABLE wires ADD COLUMN IF NOT EXISTS wire_name TEXT;                                          -- Human-readable wire name
ALTER TABLE wires ADD COLUMN IF NOT EXISTS seeded_bit INTEGER;                                      -- Set ONLY on input wires. The app writes these; nothing else.
ALTER TABLE wires ADD COLUMN IF NOT EXISTS gate TEXT;                                               -- The gate driving this wire (null for inputs)
ALTER TABLE wires ADD COLUMN IF NOT EXISTS a_wire TEXT;                                             -- self-FK: the wire feeding this gate's left input
ALTER TABLE wires ADD COLUMN IF NOT EXISTS b_wire TEXT;                                             -- self-FK: the wire feeding this gate's right input
COMMENT ON COLUMN wires.wire_id IS 'WireId';
COMMENT ON COLUMN wires.wire_name IS 'Human-readable wire name';
COMMENT ON COLUMN wires.seeded_bit IS 'Set ONLY on input wires. The app writes these; nothing else.';
COMMENT ON COLUMN wires.gate IS 'The gate driving this wire (null for inputs)';
COMMENT ON COLUMN wires.a_wire IS 'self-FK: the wire feeding this gate''s left input';
COMMENT ON COLUMN wires.b_wire IS 'self-FK: the wire feeding this gate''s right input';

-- ============================================================================
-- FK INDEXES - Performance only, always applied
-- ============================================================================

-- GateTruthRows
CREATE INDEX IF NOT EXISTS idx_gate_truth_rows_gate ON gate_truth_rows (gate);

-- Wires
CREATE INDEX IF NOT EXISTS idx_wires_gate ON wires (gate);
CREATE INDEX IF NOT EXISTS idx_wires_a_wire ON wires (a_wire);
CREATE INDEX IF NOT EXISTS idx_wires_b_wire ON wires (b_wire);

-- 4 FK index(es) declared.
